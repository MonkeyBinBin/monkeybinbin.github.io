# Contentful Content Delivery API token (required for build & runtime data fetching)
# 取得方式：Contentful 後台 → Settings → API keys → Content delivery / preview tokens
# 備註：CDA token 為唯讀 token，會被 inline 進 client bundle，屬於「公開金鑰」等級
# 但仍禁止寫死在 repo 中，應透過環境變數注入
CTF_CDA_ACCESS_TOKEN=

# 以下兩個非敏感、可於 config/index.mjs 取得預設值，只在需要覆寫時設定
# CTF_SPACE_ID=
# CTF_BLOG_POST_TYPE_ID=
