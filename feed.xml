<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>被程式設計的猴子 - 記錄日常工作、開發遇到的大小事</title>
        <link>https://monkeybinbin.github.io</link>
        <description>來自台灣的工程師，目前偏好前端開發。本網站使用 Nuxt.js 與 Bootstrap 4 搭建而成。紀錄一些日常開發程式的大小事情。</description>
        <lastBuildDate>Wed, 01 Oct 2025 03:41:03 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>zh-TW</language>
        <copyright>All rights reserved 2025, MonkeyBinBin</copyright>
        <item>
            <title><![CDATA[CSS 2025 新特性：現代網頁開發的革命性演進]]></title>
            <link>https://monkeybinbin.github.io/article/css-2025-new-features</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/css-2025-new-features</guid>
            <pubDate>Sun, 29 Jun 2025 00:00:00 GMT</pubDate>
            <description><![CDATA[## 主要新特性瀏覽器支援表

| 特性              | Chrome | Firefox | Safari | Edge |
| :---------------- | :----: | :-----: | :----: | :--: |
| 容器查詢          |  106+  |  110+   | 16.0+  | 106+ |
| 層疊層（@layer）  |  9]]></description>
            <content:encoded><![CDATA[## 主要新特性瀏覽器支援表

| 特性              | Chrome | Firefox | Safari | Edge |
| :---------------- | :----: | :-----: | :----: | :--: |
| 容器查詢          |  106+  |  110+   | 16.0+  | 106+ |
| 層疊層（@layer）  |  99+   |   97+   | 15.4+  | 99+  |
| CSS 嵌套          |  113+  |  117+   | 16.6+  | 112+ |
| 子網格（subgrid） |  117+  |   71+   | 16.0+  | 117+ |
| 錨點定位          |  127+  | 不支援  | 不支援 | 126+ |
| 視圖轉場          |  111+  | 不支援  | 17.2+  | 111+ |
| 滾動驅動動畫      |  115+  | 不支援  | 26.0+  | 115+ |
| 邏輯屬性          |  89+   |   66+   | 15.2+  | 89+  |
| Lab 色彩空間      |  113+  |  114+   | 15.2+  | 111+ |

> 註：表格中數字為大致支援版本，實際情況請參考各瀏覽器官方文件。

## CSS 核心新特性

### 容器查詢（Container Queries）：響應式設計的新紀元

容器查詢是 2025 年最具影響力的 CSS 新特性之一，它徹底改變了響應式設計的思維模式。與傳統的媒體查詢不同，容器查詢允許元素根據其父容器的大小來調整樣式，而非依賴整個視窗的尺寸。

**主要優勢：**

- **模組化設計**：每個組件都能獨立響應自身容器的變化
- **提升可維護性**：減少了對全域斷點的依賴
- **靈活性增強**：適合組件化開發和設計系統

```css
.profile-card {
  container-type: inline-size;
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
}

@container (min-width: 500px) {
  .profile-card {
    grid-template-columns: auto 1fr;
  }
}
```

### 層疊層（Cascade Layers）：樣式管理的新方法

層疊層透過 `@layer` 規則提供了對 CSS 層疊順序的精確控制。這個特性解決了大型專案中樣式衝突和特異性戰爭的問題。

**核心概念：**

- **層級優先順序**：高層級的層會覆蓋低層級的樣式，無論特異性如何
- **可預測性**：提供了更直觀的樣式覆蓋機制
- **團隊協作**：有助於大型團隊的樣式管理

```css
@layer base, components, utilities;

@layer base {
  body {
    font-family: system-ui;
  }
}

@layer components {
  .button {
    background: blue;
    color: white;
  }
}

@layer utilities {
  .bg-red {
    background: red !important;
  }
}
```

### CSS 嵌套（CSS Nesting）：程式碼組織的革新

原生 CSS 嵌套功能讓開發者可以直接在 CSS 中使用類似 Sass 的嵌套語法，無需任何預處理器。

**主要特點：**

- **改善可讀性**：嵌套結構反映了 HTML 的層級關係
- **減少重複**：避免重複父選擇器
- **原生支援**：所有現代瀏覽器都已支援

```css
.navigation {
  background: white;

  ul {
    list-style: none;
    margin: 0;

    li {
      display: inline-block;

      &:hover {
        background: #f0f0f0;
      }
    }
  }
}
```

## 進階佈局特性

### 子網格（Subgrid）：網格佈局的完善

子網格允許網格項目繼承父網格的軌道配置，解決了複雜佈局中對齊問題。

```css
.product-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}

.product-card {
  display: grid;
  grid-template-rows: subgrid;
  grid-row: span 3;
}
```

### 錨點定位（Anchor Positioning）：精確的元素定位

錨點定位提供了聲明式的方式來定位元素，特別適合工具提示和彈出視窗。

```css
.anchor {
  anchor-name: --my-anchor;
}

.tooltip {
  position: absolute;
  position-anchor: --my-anchor;
  top: anchor(bottom);
  left: anchor(center);
}
```

## 動畫與互動增強

### 視圖轉場（View Transitions）：流暢的頁面切換

視圖轉場 API 讓開發者能夠創造平順的頁面或狀態轉換效果，大幅提升使用者體驗。

```css
.page-transition {
  view-transition-name: page-content;
}

::view-transition-old(page-content) {
  animation: fade-out 300ms ease-out;
}

::view-transition-new(page-content) {
  animation: fade-in 300ms ease-in;
}
```

### 滾動驅動動畫：與滾動同步的動效

滾動驅動動畫允許動畫與滾動位置同步，創造出引人入勝的互動體驗。

```css
.scroll-animation {
  animation: rotate 1s linear;
  animation-timeline: scroll();
}

@keyframes rotate {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
```

## 色彩與視覺增強

### 邏輯屬性：國際化友善的佈局

邏輯屬性提供了基於書寫模式的樣式設定，特別適合多語言網站開發。

```css
.content {
  margin-inline-start: 20px;
  padding-block: 10px;
  border-inline-end: 1px solid #ccc;
}
```

### Lab 色彩模型函式：更豐富的色彩表現

Lab 色彩模型函式提供了更寬廣的色域，能夠表現更接近人眼所見的色彩。

```css
.vibrant-color {
  background: lab(50 20 -30);
  color: lab(90 -5 5);
}
```

## 現代 CSS 框架比較分析

### 原子化 CSS：效率與維護性的平衡

原子化 CSS（Utility-First CSS）已成為 2025 年的主流趨勢，代表性框架包括：

**Tailwind CSS**：

- **成熟生態**：擁有豐富的插件和社群支援
- **設計系統**：基於設計令牌的方法論
- **開發效率**：快速原型開發和一致性設計

**UnoCSS**：

- **按需生成**：只生成實際使用的樣式
- **極致性能**：更小的檔案大小和更快的建構速度
- **高度可客製化**：靈活的預設配置系統

### 傳統框架的現代化演進

**Bootstrap 6**：

- **CSS 自訂屬性**：更好的客製化能力
- **無障礙改善**：增強的可訪問性支援
- **現代化整合**：與現代 JS 框架的更好整合

## 開發優勢與實務應用

### 性能優化

新的 CSS 特性帶來了顯著的性能提升：

- **減少 JavaScript 依賴**：許多以前需要 JS 的功能現在可以純 CSS 實現
- **更小的檔案大小**：按需生成和樹搖優化
- **更好的快取策略**：更細粒度的樣式控制

### 開發體驗改善

- **更直觀的語法**：邏輯屬性和嵌套語法更符合開發者思維
- **更好的除錯能力**：層疊層提供了清晰的樣式來源
- **更高的可維護性**：容器查詢和模組化設計

### 響應式設計革新

- **組件級響應**：容器查詢實現真正的組件響應式
- **多維度適應**：不僅是螢幕尺寸，還包括容器大小、色彩偏好等
- **無縫體驗**：視圖轉場創造流暢的使用者體驗

## 最佳實踐建議

### 漸進式採用策略

1. **從容器查詢開始**：逐步替換媒體查詢
2. **引入邏輯屬性**：提升國際化支援
3. **善用層疊層**：改善大型專案的樣式管理

### 性能考量

- **合理使用嵌套**：避免過深的嵌套層級
- **優化動畫**：善用硬體加速和滾動驅動動畫
- **色彩空間選擇**：根據目標裝置選擇適當的色彩空間

### 團隊協作

- **建立設計系統**：使用原子化 CSS 建立一致的設計語言
- **文件化規範**：清楚記錄層疊層的使用規則
- **工具鏈整合**：配置適當的建構工具和程式碼檢查

## 結論

CSS 2025 的新特性標誌著網頁開發進入了一個新時代。容器查詢、層疊層、CSS 嵌套等核心特性不僅提升了開發效率，更重要的是改變了我們思考和組織樣式的方式。配合現代 CSS 框架的演進，開發者現在擁有了前所未有的工具來創造出既美觀又高效的網頁應用程式。

這些新特性的採用不僅能提升開發體驗，更能為使用者帶來更流暢、更具互動性的網頁體驗。隨著瀏覽器支援度的不斷提升，現在正是開始探索和應用這些新技術的最佳時機。]]></content:encoded>
            <category>CSS</category>
        </item>
        <item>
            <title><![CDATA[Nuxt 系列 - #7 啟用 ESLint 跟 stylelint 保持良好的 Coding Style]]></title>
            <link>https://monkeybinbin.github.io/article/a-series-of-nuxtjs-07</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/a-series-of-nuxtjs-07</guid>
            <pubDate>Wed, 01 Jul 2020 00:00:00 GMT</pubDate>
            <description><![CDATA[#### 安裝 Visual Studio Code 擴充套件
* [ESLint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint) - 整合 ESLint 到 VS Code，在開發過程中會馬上顯示不符合規範的 javascript 程式碼，並且提供自動修正程式碼功能。  
  ![eslin]]></description>
            <content:encoded><![CDATA[#### 安裝 Visual Studio Code 擴充套件
* [ESLint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint) - 整合 ESLint 到 VS Code，在開發過程中會馬上顯示不符合規範的 javascript 程式碼，並且提供自動修正程式碼功能。  
  ![eslint](//images.ctfassets.net/64scck6nxm6l/3wxlR2mx6ixUiOig6FqFeN/225dd21f0f0830ca07568f49c847a24f/eslint.jpg)
* [stylelint](https://marketplace.visualstudio.com/items?itemName=stylelint.vscode-stylelint) - 整合 stylelint 到 VS Code，在開發過程中會馬上顯示不符合規範的 CSS/SCSS/Less 程式碼，並且提供自動修正程式碼功能。  
  ![stylelint](//images.ctfassets.net/64scck6nxm6l/6ICNKlVSQ5ZC9Wyw1xPXfR/1c40013e31aa9b2def36564402ccbf01/stylelint.jpg)

以上兩個擴充套件在前端專案都是必備的工具，工具非常有彈性可視需要自行調整規範。前端專案都應該要使用這兩個工具來規範程式碼風格，並依照自已團隊的需要調整制定自已的規範。

#### ESLint 安裝設定
目前專案選用規則
* ESLint 推薦規則 - https://eslint.org/docs/rules/ ，網頁內清單前方有打勾的項目
* Vue.js 官方推薦規則 - https://eslint.vuejs.org/rules/ ，網頁內 Priority C: Recommended for Vue.js 2.x 還有 Uncategorized 的項目
* JavaScript Standard Style 規則 - https://standardjs.com/rules.html

##### Step 1：安裝 ESLint 與 webpack loader 套件
``` bash
npm install --save-dev babel-eslint eslint eslint-loader
```

##### Step 2：安裝 Vue.js 官方制定的檢查規則套件
``` bash
npm install --save-dev eslint-plugin-vue
```

##### Step 3：安裝 Javascript Standard Style 檢查規則套件
``` bash
npm install --save-dev eslint-config-standard eslint-plugin-standard eslint-plugin-promise eslint-plugin-import eslint-plugin-node
```
步驟1~3可以一次安裝，分開寫是為了更清楚了解安裝套件的用途為何

##### Step 4：於專案根目錄建立 ESLint 設定檔 .eslintrc.js，設定內容如下
``` javascript
module.exports = {
  root: true,
  env: {
    browser: true,
    node: true
  },
  parserOptions: {
    parser: 'babel-eslint'
  },
  extends: [
    'eslint:recommended',
    'plugin:vue/recommended',
    'standard'
  ],
  plugins: [
    'vue'
  ],
  rules: {},
  globals: {
    $: 'readonly',
    moment: 'readonly'
  }
}
```  
extends 設定啟用檢查的規則，可使用共享配置的套件或 plugins 的設定。共享配置的套件名稱的前綴會是 eslint-config-，設定時可以省略前綴。plugins 設定則為 plugin:套件名稱去掉前綴/規則組合(例如：plugin:vue/recommended)。

plugins 設定要使用的 plugins 套件，一般套件名稱的前綴會是 eslint-plugins-，設定時可以省略前綴進行設定，此處的 vue 指的就是第二步驟安裝的 eslint-plugins-vue

rules 已啟用的規則需要調整的話可以在這邊增加設定，這裡的設定會覆蓋原設定

globals 用來設定全域載入的套件。由於在 nuxt.config.js 有設定 webpack.ProvidePlugin 載入 jquery 跟 moment 因此在所有程式碼都可直接使用 $ 與 moment 不需另外 import

#### stylelint 安裝設定
目前專案選用規則
* stylelint 預設規則 - https://stylelint.io/user-guide/rules/about
* 搭配 stylelint 規則使用的 scss 規則 - https://github.com/kristerkari/stylelint-scss#readme
* stylelint-config-standard 規則 - https://github.com/stylelint/stylelint-config-standard
* stylelint-config-prettier - https://github.com/prettier/stylelint-config-prettier#readme ，排除與 prettier 衝突的規則(prettier 為用來排版的 vs code 擴充套件)
* Sass Guidelines - https://sass-guidelin.es/
* 樣式屬性依首字字母排序(a 到 z) - https://github.com/hudochenkov/stylelint-order#readme

##### Step 1：安裝 stylelint 與 scss 規則套件
``` bash
npm install --save-dev stylelint stylelint-scss
```

##### Step 2：安裝 stylelint-config-standard 規則套件
``` bash
npm install --save-dev stylelint-config-standard
```

##### Step 3：安裝排除與 prettier 衝突的規則套件
``` bash
npm install --save-dev stylelint-config-prettier
```

##### Step 4：安裝樣式屬性依首字字母排序(a 到 z)套件
``` bash
npm install --save-dev stylelint-order
```

##### Step 5：於專案根目錄建立 stylelint 設定檔 .stylelintrc.json，設定內容如下
``` json
{
  "extends": [
    "stylelint-config-standard",
    "stylelint-config-prettier",
    "stylelint-config-sass-guidelines"
  ],
  "plugins": ["stylelint-scss", "stylelint-order"],
  "rules": {
    "max-nesting-depth": 3
  },
  "ignoreFiles": [
    "./node_modules/**/*.{css,scss,sass}",
    "./dist/**/*.{css,scss,sass}"
  ]
}
```
extends 設定啟用檢查的規則，可使用共享配置的套件

plugins 增加 stylelint 規則的套件

rules 已啟用的規則需要調整的話可以在這邊增加設定，這裡的設定會覆蓋原設定

ignoreFiles 排除要檢查的檔案，目前排除的是 npm 安裝的套件與 webpack 打包後產出的資料夾

#### Visual Studio Code 設定
工作區設定(/.vscode/settings.json) - 啟用儲存檔案時，自動修正 eslint 與 stylelint 問題
``` json
{
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true,
    "source.fixAll.stylelint": true
  }
}
```

#### 增加 lint 指令
開啟 package.json 檔案，在 scripts 內增加以下設定  
``` json
{
  "scripts": {
    "lint:js": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "lintfix:js": "eslint --fix --ext .js,.vue --ignore-path .gitignore .",
    "lint:css": "npx stylelint '**/*.{css,scss,sass,vue}'",
    "lintfix:css": "npx stylelint '**/*.{css,scss,sass,vue}' --fix"
  }
}
```

使用指令執行 eslint 檢查  
``` bash
npm run lint:js
```

使用指令執行 eslint 檢查，並修復可自動修復問題
``` bash
npm run lintfix:js
```

使用指令執行 stylelint 檢查  
``` bash
npm run lint:css
```

使用指令執行 stylelint 檢查，並修復可自動修復問題
``` bash
npm run lintfix:css
```

注意事項：要使用 stylelint 指令需要將 nodejs 版本必需為 10.13 以上(含10.13)版本，因為目前的 stylelint 版本只支援 nodejs 10.13 以上，但這不影響 vs code 擴充套件 stylelint 檢查的結果。在較低版本的 nodejs 還是可以使用 vs code 擴充套件 stylelint 來檢查，只是規則的來源是 .stylelintrc.json 的設定。

#### 參考資料
[Nuxt.js - ESLint and Prettier](https://nuxtjs.org/guide/development-tools#eslint-and-prettier)  
[ESLint](https://eslint.org/)  
[JavaScript Standard Style](https://standardjs.com/)  
[eslint-plugin-vue](https://eslint.vuejs.org/)  
[eslint-config-standard](https://github.com/standard/eslint-config-standard)  
[stylelint](https://stylelint.io/)  
[stylelint-config-standard](https://github.com/stylelint/stylelint-config-standard)  
[stylelint-config-prettier](https://github.com/prettier/stylelint-config-prettier)  
[stylelint-config-sass-guidelines](https://github.com/bjankord/stylelint-config-sass-guidelines)  
[stylelint-order](https://github.com/hudochenkov/stylelint-order)  
[stylelint-scss](https://github.com/kristerkari/stylelint-scss)  
]]></content:encoded>
            <category>Nuxt.js</category>
        </item>
        <item>
            <title><![CDATA[Nuxt 系列 - #6 整合 CMS 服務 contentful]]></title>
            <link>https://monkeybinbin.github.io/article/a-series-of-nuxtjs-06</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/a-series-of-nuxtjs-06</guid>
            <pubDate>Thu, 04 Jun 2020 00:00:00 GMT</pubDate>
            <description><![CDATA[#### 註冊/登入 contentful
contentful 網站 [https://www.contentful.com/](https://www.contentful.com/)  
![contentful website](//images.ctfassets.net/64scck6nxm6l/26a2exirWOGmwk30A7Y9BH/d5e4cd4d457c477af7f86e]]></description>
            <content:encoded><![CDATA[#### 註冊/登入 contentful
contentful 網站 [https://www.contentful.com/](https://www.contentful.com/)  
![contentful website](//images.ctfassets.net/64scck6nxm6l/26a2exirWOGmwk30A7Y9BH/d5e4cd4d457c477af7f86e6bfa0520fc/contentful_website.jpg)

註冊頁 [https://www.contentful.com/sign-up/](https://www.contentful.com/sign-up/)  
可使用 Github、Google 帳號登入或直接填表單進行註冊  
![contentful signup](//images.ctfassets.net/64scck6nxm6l/54tBwxmDE80y7GlICKFqHG/1140995b967a9d9d5f4322b10d41347f/contentful_signup.jpg)

#### 建立 Space
建立方案的第一個步驟選擇類型，如未填寫付款資訊就不能選擇需付費的方案  
這邊就直接選擇 Free 的方案，上面也會清楚標示可用上限之資訊  
![create space 1](//images.ctfassets.net/64scck6nxm6l/Pp95dHDApE6wGqn04rl28/c58111aa61c1707e418107e6ad168bd7/create_space_1.jpg)

步驟二就是給 Space 一個名字，並選擇建立一個空的 space  
![create space 2](//images.ctfassets.net/64scck6nxm6l/1U1qExvH0Dl5NsubVnizB6/5fd213afd7e19175dd815fbfa438f957/create_space_2.jpg)

步驟三確認後 Space 就建立完成  
![create space 3](//images.ctfassets.net/64scck6nxm6l/7BsEepzYQAhgcW1CeDqwx1/874bea2a71b62be238ac2ac7b78d7404/create_space_3.jpg)

#### 在 Content model 建立 content type
點選畫面上方的 Content model 功能，新增 content type  
相當於建立一個資料表的欄位名稱與型態  
一個 content type 最多可定義50個欄位，資料型態也有11種  
每種資料型態都會有一些限制，可能是長度、數字最大最小值、日期時間格式…等等  
詳細規格可以直接參考 [Content data model](https://www.contentful.com/developers/docs/concepts/data-model/)，裡面有非常詳細的說明  

新增欄位時，介面提供以下幾種欄位型態  
![content model field](//images.ctfassets.net/64scck6nxm6l/6UzDsbhnOcpMTuFbv5LYbh/ee4a16645a271949a98a8a8607e2206b/content_model_field.jpg)  
欄位全部設定完成後，按下畫面右側 Save 即可開始依照剛剛的欄位設定建立資料內容

目前 blog 文章的欄位規畫一共有6個欄位，如下圖所示  
![blog post model field](//images.ctfassets.net/64scck6nxm6l/5cKrtOK8s2qyIBoSKGCGOU/9f1de59e345fd6d5cbfcba67f342b177/blog_post_model_field.jpg)  
主要的文章內容儲存於 Article Content 欄位，內容格式使用 markdown 所以欄位型態選擇 Long Text 即可  
文章分類標籤儲存於 Category List 欄位，由於可能有多個分類，所以在設定時要特別勾選是多筆。  
**使用此方式的好處是後續在使用 api 時，可以直接使用參數的方式過濾資料，因此可節省資料傳輸的大小。如果使用 Json object 方式儲存，則需要將所有資料撈回再過濾。**  
如下圖所示紅框部份要勾選  
![text field with list](//images.ctfassets.net/64scck6nxm6l/7yZFS4uRHy4HR9hLcEuKly/7f3dc7f4020160c499fbff26cd20d4ec/text_field_with_list.jpg)

#### 新增 Content
點選畫面上方的 Content 功能，新增指定 content type 的資料內容  
新增資料內容頁面會依據先前建立的 content type 呈現表單  
編輯過程中會自動存檔  
不同欄位型態會呈現不同輸入方式(日期選單、markdown 編輯器)，也有基本檢核(例如長度、必填)  
![add content](//images.ctfassets.net/64scck6nxm6l/viO2V70c50nbD0j0msSTu/0d5937a6a412c4639dc7f3c9baa29bf5/add_content.jpg)  
完成編輯後可選擇排程或是立即 Publish  

#### 設定 API Key
點選畫面上方的 Settings 選擇 API keys 開啟設定畫面，按下 "Add API key"  
![add api key](//images.ctfassets.net/64scck6nxm6l/6LN5A4npOzeVbRxiQ9Jy07/17161f0a9241877802134390ff77c8fd/add_api_key.jpg)  
編輯完成後按下 Save 即完成設定  
畫面上有3個不可編輯的欄位，由系統自動產生的資訊，分別是
* Space ID
* Content Delivery API - access token
* Content Preview API - access token  

後續在專案中呼叫 contentful api 時，需要用到 Space ID 與 access token，其中 Content Delivery API - access token 是用來取得正式發佈的資料，而 Content Preview API - access toke 除了取得正式發佈的資料還有也會取得狀態為 "Draft"的資料。方便你在開發環境使用，可快速切換。

#### 在 Nuxt.js 專案中使用 contentful 服務
執行下列指令安裝 contentful 套件
``` bash
npm install contentful --save
```

在 plugins 資料夾新增一個檔案 contentful.js 用來建立呼叫 contentful api 的 client  
建立時必需提供 space id 與 access token  
如果有切換環境的需要，則可設定 environment 屬性，此設定預設值為 master  
``` javascript
// plugins/contentful.js

const contentful = require('contentful')
// use default environment config for convenience
// these will be set via `env` property in nuxt.config.js
const config = {
  space: process.env.CTF_SPACE_ID,
  accessToken: process.env.CTF_CDA_ACCESS_TOKEN
}

// export `createClient` to use it in page components
module.exports = {
  createClient () {
    return contentful.createClient(config)
  }
}
```

在 nuxt.config.js 設定環境變數 CTF_SPACE_ID、CTF_CDA_ACCESS_TOKEN 與 CTF_BLOG_POST_TYPE_ID，供程式中取用  
``` javascript
// ./nuxt.config.js
const config = require('./config')

module.exports = {
  // ...
  env: {
    CTF_SPACE_ID: config.CTF_SPACE_ID,
    CTF_CDA_ACCESS_TOKEN: config.CTF_CDA_ACCESS_TOKEN,
    CTF_BLOG_POST_TYPE_ID: config.CTF_BLOG_POST_TYPE_ID
  }
  // ...
}
```

利用 plugins/contentful.js 的 createClient 方法建立呼叫 api 的物件使用 getEntries 進行 api 呼叫  
以下為簡易的呼叫 api 範例  
``` javascript
import contentful from '~/plugins/contentful.js'
const client = contentful.createClient()

const getArticles = () => {
  return client.getEntries({
    // 設定要取得的 content type id
    content_type: config.CTF_BLOG_POST_TYPE_ID,
    // 指定呼叫 api 回傳的欄位有那些
    select: 'fields.id,fields.createDate,fields.title,fields.slug,fields.categoryList',
    // 指定回傳資料排序的欄位與排序方式(由大至小 or 由小至大)
    order: '-fields.createDate'
  })
    .then(res => map(res.items, item => item.fields))
    .catch(() => Promise.resolve([]))
}
```

完成以上步驟之後即可在 Nuxt.js 專案內呼叫 contentful 的 api  
為了方便維護與管理我在專案中建立 services/api.js，並且於檔案內建立需使用的呼叫 api 方法提供程式使用  

#### blog 專案使用 getEntries 查詢資料參數使用說明
在 Content 新增的資料稱為 Entry，而 getEntries 則是用來取得這些 Content 資料的方法，回傳為 Promise 物件  
getEntries 方法提供了非常多的參數可供開發人員設定使用，以下針對目前專案有使用的參數進行說明  
其他詳細的使用方式也可參考[官方文件](https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters)，點擊參數說明內的"Query entries"可看到該參數使用的程式碼(可選平台)並且模擬查詢(參數可自行調整)

##### 首頁文章清單資料
1. 顯示最新10筆資料
2. 文章排列方式為由新到舊
3. 不回傳"文章內容"欄位資料，節省傳輸量

``` javascript
import contentful from '~/plugins/contentful.js'
const client = contentful.createClient()
client.getEntries({
    content_type: 'post',
    select: 'fields.id,fields.createDate,fields.title,fields.slug,fields.categoryList',
    order: '-fields.createDate',
    limit: 10
})
```

##### 依分類搜尋文章清單資料
1. 取得所有文章分類包含"Nuxt.js"的文章資料
2. 文章排列方式為由新到舊
3. 不回傳"文章內容"欄位資料，節省傳輸量

``` javascript
import contentful from '~/plugins/contentful.js'
const client = contentful.createClient()
client.getEntries({
    content_type: 'post',
    select: 'fields.id,fields.createDate,fields.title,fields.slug,fields.categoryList',
    order: '-fields.createDate',
    'fields.categoryList': 'Nuxt.js'
})
```

##### 使用 id 取得文章詳細內容頁的資料
1. 取得 id 為"a-series-of-nuxtjs-01"的文章資料
2. 回傳所有欄位

``` javascript
import contentful from '~/plugins/contentful.js'
const client = contentful.createClient()
client.getEntries({
    content_type: 'post',
    'fields.id': 'a-series-of-nuxtjs-01'
})
```

#### 參考資料
[contentful](https://www.contentful.com/)  
[contentful Guides](https://www.contentful.com/help/contentful-101/)  
[contentful Content data model](https://www.contentful.com/developers/docs/concepts/data-model/)  
[contentful API reference](https://www.contentful.com/developers/docs/references/)  
[contentful.js - Contentful JavaScript Delivery SDK](https://www.npmjs.com/package/contentful)  
[Integrate Contentful with Nuxt.js](https://www.contentful.com/developers/docs/javascript/tutorials/integrate-contentful-with-vue-and-nuxt/)]]></content:encoded>
            <category>Nuxt.js</category>
        </item>
        <item>
            <title><![CDATA[Nuxt 系列 - #5 Pages 開發大小事]]></title>
            <link>https://monkeybinbin.github.io/article/a-series-of-nuxtjs-05</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/a-series-of-nuxtjs-05</guid>
            <pubDate>Tue, 19 May 2020 00:00:00 GMT</pubDate>
            <description><![CDATA[#### Async Data
Nuxt.js 增加了一個 asyncData 的方法，在 Vue 組件初始化前執行，用來取得 api 資料，因此在這個方法內無法使用 this 來引用組件的實體對象，僅能使用傳入的 [context](https://nuxtjs.org/api/context) 來獲得相關的資訊與方法。官方建議呼叫 api 使用 axios 套件，因此接下來的程式碼範例將會使用]]></description>
            <content:encoded><![CDATA[#### Async Data
Nuxt.js 增加了一個 asyncData 的方法，在 Vue 組件初始化前執行，用來取得 api 資料，因此在這個方法內無法使用 this 來引用組件的實體對象，僅能使用傳入的 [context](https://nuxtjs.org/api/context) 來獲得相關的資訊與方法。官方建議呼叫 api 使用 axios 套件，因此接下來的程式碼範例將會使用 axios。

asyncData 使用方式有兩種，可以挑選自已熟悉的方式來使用，方法如下：
1. 返回 Promise
    ``` javascript
    export default {
        asyncData ({ params }) {
            return axios.get(`https://my-api/posts/${params.id}`)
            .then((res) => {
                return { title: res.data.title }
            })
        }
    }
    ```
2. 使用 async、await
    ``` javascript
    export default {
        async asyncData ({ params }) {
            const { data } = await axios.get(`https://my-api/posts/${params.id}`)
            return { title: data.title }
        }
    }
    ```

asyncData 回覆的結果將會與 Vue 組件中 data 方法回傳的 json 物件合併，提供給組件使用。與開發一般 Vue 應用程式時，通常都在 mounted 生命周期中去呼叫 api 有些許不同。

asyncData 傳入的 context 包含三大類可用屬性與方法，分別為 Universal、Server-side、Client-side。

除了 asyncData 有 context 可用，還有其他 Nuxt.js 的方法 fetch、plugins、middleware、nuxtServerInit 也都會傳入 context。在上述這些方法中還可以使用 process.server 判斷是否為 Server-side 或 process.client 判斷是否為 Client-side。

以下程式碼可以看出目前 context 內包含了那些屬性與方法  
``` javascript
function (context) {
    // Universal keys
    const {
        app,
        store,
        route,
        params,
        query,
        env,
        isDev,
        isHMR,
        redirect,
        error
    } = context
    // Server-side
    if (process.server) {
        const { req, res, beforeNuxtRender } = context
    }
    // Client-side
    if (process.client) {
        const { from, nuxtState } = context
    }
}
```

如何在 asyncData 取得動態路由傳入的參數？  
解構 context 取得 params，"params.參數名稱"即可取得傳入參數值   
``` javascript
export default {
    asyncData ({ params }) {
        console.log('pages/article/_id.vue 傳入參數 id => ', params.id)
    }
}
```

錯誤處理  
解構 context 取得 error 方法，當需要拋出錯誤時呼叫 error 方法，Nuxt.js 就會顯示錯誤頁面  
``` javascript
export default {
    asyncData ({ params, error }) {
        return axios.get(`https://my-api/posts/${params.id}`)
            .then((res) => {
                return { title: res.data.title }
            })
            .catch((e) => {
                error({ statusCode: 404, message: 'Post not found' })
            })
    }
}
```

#### Plugins
當需要使用 Vue.use、Vue.filter、Vue.component 時，應在 plugins 資料夾建立程式檔案並且在 nuxt.config.js 增加載入 plugins 的設定。

目前專案有使用 [bootstrap-vue](https://bootstrap-vue.org/)、[vue-disqus](https://github.com/ktquez/vue-disqus#readme) 與 [@fortawesome/vue-fontawesome](https://github.com/FortAwesome/vue-fontawesome) 套件，另外也增加了一些 filter 的設定，都是透過 plugins 方式載入。套件的使用方式基本上都參照套件內的說明，filter 的設定方式也跟原本 Vue 的寫法一樣，只是在 Nuxt.js 需要在 nuxt.config.js 中設定 plugins。

目前 blog 文章內容格式是使用 Markdown，因此必需將 Markdown 轉換為 HTML 的內容，利用 [marked](https://marked.js.org/#/README.md#README.md) 套件來做轉換，搭配 Vue filter 的方式使用。另外還有增加一個顯示日期的 filter。

新增 plugins/filters.js 檔案  
``` javascript
import Vue from 'vue'
import highlightjs from 'highlight.js'
import marked, { Renderer } from 'marked'
import pathHelper from '~/helpers/path'

// Create your custom renderer.
const renderer = new Renderer()
renderer.link = (href, title, text) => `<a target="_blank" href="${href}" title="${title}">${text}</a>`
renderer.table = (header, body) => `<table class="table table-striped">${header}${body}</table>`

// Set the renderer to marked.
marked.setOptions({
        renderer,
        baseUrl: pathHelper.getBaseUrl(),
        highlight: function (code, language) {
        // Check whether the given language is valid for highlight.js.
        const validLang = !!(language && highlightjs.getLanguage(language))
        // Highlight only if the language is valid.
        const highlighted = validLang ? highlightjs.highlight(language, code).value : code
        // Render the highlighted code with `hljs` class.
        return `<pre><code class="hljs ${language}">${highlighted}</code></pre>`
    }
})

Vue.filter('parseMd', marked)

Vue.filter('parseDatetime', function (datetime) {
    const parseDate = moment(datetime, 'YYYY-MM-DD')
    return parseDate.format('LL')
})
```

在 nuxt.config.js 增加 plugins 設定  
``` javascript
export default {
    plugins: [
        '~/plugins/filters.js',
        // or
        { src: '~/plugins/filters.js' }
    ]
}
```

載入 plugins 也是可以選擇要在 client、server 或者都要使用，只需要在 nuxt.config.js 設定即可。設定方式有2種：
1. 傳入物件包含 src 與 mode 設定  
    ``` javascript
    export default {
        plugins: [
            { src: '~/plugins/both-sides.js' }, // both client & server
            { src: '~/plugins/client-only.js', mode: 'client' }, // only in client side
            { src: '~/plugins/server-only.js', mode: 'server' } // only in server side
        ]
    }
    ```
2. 檔案命名  
    ``` javascript
    export default {
        plugins: [
            '~/plugins/baz.js', // both client & server
            '~/plugins/foo.client.js', // only in client side
            '~/plugins/bar.server.js' // only in server side
        ]
    }
    ```

#### Assets
Nuxt 預設使用 file-loader 與 url-loader 來處理圖片與字型檔案的載入與使用。如果有不需要透過 webpack 處理的靜態資源檔案，可以放置在 static 資料夾，執行打包後會將檔案複製到產出結果的資料夾。需要透過 webpack 處理的圖片與字型檔案則統一放置於 assets 資料夾。

Webpack Assets 檔案位置：  
assets/  
&emsp;∟ image.png

Webpack Assets 使用方式：
``` javascript
<template>
  <img src="~/assets/image.png">
</template>
```

Static Assets 檔案位置：  
static/  
&emsp;∟ image.png

Static Assets 使用方式：
``` javascript
<template>
  <img src="/image.png">
</template>
```

注意事項：  
從 Nuxt 2.0 後，在 css 使用路徑別名 \~/ 時必需移除斜線，才能正確的載入檔案。例如：background: url("~assets/img/avatar.jpg");

#### 樣式配置
使用 SCSS 撰寫樣式  

SCSS 樣式檔案結構  
assets/  
&emsp;∟ sass/  
&emsp;&emsp;∟ helpers/  
&emsp;&emsp;&emsp;∟ \_functions.scss  
&emsp;&emsp;&emsp;∟ \_mixins.scss  
&emsp;&emsp;&emsp;∟ \_variables.scss  
&emsp;&emsp;∟ main.scss  

將 SCSS functions、mixins 與 variables 分檔案放置方便管理與維護   
Global 樣式放置於 assets/sass/main.scss  
Pages 與 Components 自身樣式設定於各自的 .vue 檔案內，並且啟用 scoped 設定

目前規畫的 \_variables.scss 內容，主要是顏色的設定  
``` scss
$primary-color: #1dc8cd;
$secondary-color: lighten(saturate($primary-color, 25), 50);
$tertiary-color: #1de099;
$marked-primary-color: #c91414;
$marked-secondary-color: lighten(saturate($marked-primary-color, 25), 50);
```

若要讓 \_functions.scss、\_mixins.scss 與 \_variables.scss 能夠在整個應用程式的樣式中使用，必需使用 [@nuxtjs/style-resources](https://github.com/nuxt-community/style-resources-module#readme) 套件引入上述3個檔案

@nuxtjs/style-resources 使用方式：  
1. 安裝 @nuxtjs/style-resources 套件
    ``` shell
    npm add -D @nuxtjs/style-resources
    ```
2. 在 nuxt.config.js 增加 buildModules 與 styleResources 設定  
    ``` javascript
    export default {
        buildModules: [
            '@nuxtjs/style-resources',
        ],
        styleResources: {
            // your settings here
            scss: [
                path.resolve(__dirname, 'assets/sass/helpers/_variables.scss'),
                path.resolve(__dirname, 'assets/sass/helpers/_functions.scss'),
                path.resolve(__dirname, 'assets/sass/helpers/_mixins.scss')
            ]
        }
    }
    ```

設置完成後即可在任何 .scss 檔案、Pages 與 Components 使用 functions、mixins 與 variables。以下為目前 blog 專案內 pages/article/_id.vue style 設定範例  
``` javascript
<style lang="scss" scoped>
.md-content {
    & /deep/ img {
        max-width: 100%;
    }
}
.article__date::after {
    content: "";
}
.other_article_link {
    color: $primary-color;
}
</style>
```

最後在 nuxt.config.js 設定全站要使用的所有樣式檔案  
``` javascript
export default {
    css: [
        '@fortawesome/fontawesome-svg-core/styles.css',
        // 載入bootstrap
        'bootstrap/scss/bootstrap.scss',
        'bootstrap-vue/dist/bootstrap-vue.css',
        // 載入highlight.js樣式(可選擇不同theme)
        'highlight.js/styles/zenburn.css',
        // 載入aos樣式
        'aos/src/sass/aos.scss',
        // 主要css樣式(customer)
        '~/assets/sass/main.scss'
    ]
}
```

針對 Pages 的說明大概到這邊，下一篇要介紹內容管理的服務 [contentful](https://www.contentful.com/)，如何在上面建立、管理 blog 的內容與如何跟 Nuxt.js 整合。

#### 參考資料
[Nuxt.js - Async Data](https://nuxtjs.org/guide/async-data)  
[Nuxt.js - Assets](https://nuxtjs.org/guide/assets)  
[Nuxt.js - Plugins](https://nuxtjs.org/guide/plugins)  
[@nuxtjs/style-resources](https://github.com/nuxt-community/style-resources-module#readme)
]]></content:encoded>
            <category>Nuxt.js</category>
        </item>
        <item>
            <title><![CDATA[Nuxt 系列 - #4 自訂 HTML 模板、Header、佈局與 Pages]]></title>
            <link>https://monkeybinbin.github.io/article/a-series-of-nuxtjs-04</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/a-series-of-nuxtjs-04</guid>
            <pubDate>Fri, 01 May 2020 00:00:00 GMT</pubDate>
            <description><![CDATA[#### Nuxt 視圖架構
了解 Nuxt 的構成為何，理解各層可使用的方法與設定，幫助在開發時能夠製作出更符合需求的設計，並且能夠適當分配排版佈局與組件切分。

![nuxt views schema](//images.ctfassets.net/64scck6nxm6l/5hDSofDCIxrzOhz3fwVp5u/c7e787138bf8ffafa2784094ec91db98/nuxt]]></description>
            <content:encoded><![CDATA[#### Nuxt 視圖架構
了解 Nuxt 的構成為何，理解各層可使用的方法與設定，幫助在開發時能夠製作出更符合需求的設計，並且能夠適當分配排版佈局與組件切分。

![nuxt views schema](//images.ctfassets.net/64scck6nxm6l/5hDSofDCIxrzOhz3fwVp5u/c7e787138bf8ffafa2784094ec91db98/nuxt-views-schema.svg)

#### App Template
Nuxt 預設的模板如下

``` html
<!DOCTYPE html>
<html {{ HTML_ATTRS }}>
  <head {{ HEAD_ATTRS }}>
    {{ HEAD }}
  </head>
  <body {{ BODY_ATTRS }}>
    {{ APP }}
  </body>
</html>
```

##### 如何自訂 HTML 模板？
於專案的根目錄資料夾新增一個 **app.html** 的檔案。並調整 html 文件的內容，記得與上述預設模版一樣，將 HTML\_ATTRS、HEAD\_ATTRS 與 BODY\_ATTRS 分別置入 html、head 還有 body 的屬性。

以下為官方提供的範例，添加 IE 的條件表達式

``` html
<!DOCTYPE html>
<!--[if IE 9]><html lang="en-US" class="lt-ie9 ie9" {{ HTML_ATTRS }}><![endif]-->
<!--[if (gt IE 9)|!(IE)]><!--><html {{ HTML_ATTRS }}><!--<![endif]-->
  <head {{ HEAD_ATTRS }}>
    {{ HEAD }}
  </head>
  <body {{ BODY_ATTRS }}>
    {{ APP }}
  </body>
</html>
```

#### HTML Head
利用 [vue-meta](https://vue-meta.nuxtjs.org/) 套件，更新 document head 與 meta， vue-meta 套件還有非常多強大的功能，可以花點時間研究。

Nuxt.js 預設 vue-meta 的參數配置如下

``` javascript
{
  keyName: 'head', // 設定 meta 資訊在 vue 組件內存在的物件名稱，vue-meta 會使用這個 key 取得設定值
  attribute: 'n-head', // vue-meta 在監聽標簽時所添加的屬性名稱
  ssrAttribute: 'n-head-ssr', // 讓 vue-meta 知道 meta 已完成伺服器端渲染的屬性名稱
  tagIDKeyName: 'hid' // vue-meta 用來決定是否覆蓋或增加 tag 的屬性名稱
}
```

可在 nuxt.config.js 定義所有 head 預設的標籤(title、meta、link、script)，這樣所有的頁面都會套用。  
由於文章內容頁需要自行定義 keywords、description 等設定，所以有一部份的 meta 有設定 hid 屬性，讓子組件可以進行覆蓋。

#### Layouts
功能相當於一般 vue 專案中我們會建立的 App.vue 檔案，用來定義最底層的 html 排版。預設檔案為 layouts/default.vue。

預設的程式碼如下

``` javascript
<template>
  <nuxt/>
</template>
```

Nuxt 另外提供了自定義 Layouts 的方式
1. 於 layouts 資料夾內建立 .vue 檔案。
2. 建立 pages 時，在 script 中增加 layout 屬性指定為上一步驟建立的 vue 檔案名稱。

以下為官方範例  
建立一個 layouts/blog.vue

``` javascript
<template>
  <div>
    <div>My Blog</div>
    <nuxt/>
  </div>
</template>
```

於頁面 pages/posts/index.vue 使用自定義的 layout

``` javascript
<template>
<!-- Your template -->
</template>
<script>
export default {
  layout: 'blog'
  // page component definitions
}
</script>
```

錯誤頁面也可自訂，在 layouts 資料夾內建立 error.vue 檔案來客製錯誤頁面。  
error.vue 雖然此檔案放置於 layouts 資料夾，但 nuxt 把它視為 page，所以 template 中不需包含 <nuxt/\>，並且會在指定的 layout 中 <nuxt/\> 區塊呈現(未指定則為預設)。  
當應用程式發生 404、500…等錯誤時，就會顯示此錯誤頁面，並且透過 props 將當次錯誤資訊傳遞至 error.vue 頁面內使用。

Nuxt 預設錯誤頁面程式碼：[nuxt-error.vue](https://github.com/nuxt/nuxt.js/blob/dev/packages/vue-app/template/components/nuxt-error.vue)

以下為官方範例  
透過 props 的 error 傳遞當次錯誤資訊，提供頁面使用  
error 為 Object，預設內含 message、path 與 statusCode，屬性也可透過 context.error 自訂 

``` javascript
<template>
  <div class="container">
    <h1 v-if="error.statusCode === 404">Page not found</h1>
    <h1 v-else>An error occurred</h1>
    <nuxt-link to="/">Home page</nuxt-link>
  </div>
</template>

<script>
export default {
  props: ['error']
}
</script>
```

#### Pages
每個 page 組件實際上就是 vue 組件，只是增加了一些 Nuxt 特殊的配置項目，讓開發者能夠快速開發通用於 SSR 與 CSR 的應用程式。

``` javascript
<template>
  <h1>Hello {{ name }}!</h1>
</template>

<script>
export default {
  asyncData (context) {
    // called every time before loading the component
    // as the name said, it can be async
    // Also, the returned object will be merged with your data object
    return { name: 'World' }
  },
  fetch () {
    // The `fetch` method is used to fill the store before rendering the page
  },
  head () {
    // Set Meta Tags for this Page
  },
  // and more functionality to discover
  ...
}
</script>
```

Nuxt 為 page 提供的特殊配置項目

* [asyncData](https://nuxtjs.org/guide/async-data) - 當需要獲取資料並呈現，在初始化組件之前處理非同步資料操作，並且會與組件的 data 合併，此方法第一個參數為當前頁面組件的 context。
* [fetch](https://nuxtjs.org/api/pages-fetch) - 在頁面 render 之前將資料寫入 Vuex store，與 asyncData 方法類似，不一樣的是 fetch 不會設置組件的 data，此外 nuxt 2.12 以後的版本(包含2.12)的使用方法與先前有些不同，使用上要特別注意。
* [head](https://nuxtjs.org/api/pages-head) - 配置目前頁面的 head 標籤。
* [layout](https://nuxtjs.org/api/pages-layout) - 指定在 layouts 目錄中定義的 vue 檔案。
* [loading](https://nuxtjs.org/api/configuration-loading) - 定義目前頁面的 loading 效果，也可關閉之後透過 this.$nuxt.$loading.finish() 與 this.$nuxt.$loading.start() 手動控制此效果。
* [transition](https://nuxtjs.org/api/pages-transition) - 指定頁面切換時的轉場特效的名稱，需搭配樣式設定。
* [scrollToTop](https://nuxtjs.org/api/pages-scrolltotop#the-scrolltotop-property) - 設定目前頁面在渲染頁面前是否要將畫面滾動至頂部。
* [validate](https://nuxtjs.org/api/pages-validate#the-validate-method) - 驗証動態路由參數的方法，防止非預期的參數值傳入。
* [middleware](https://nuxtjs.org/guide/routing#middleware) - 在頁面渲染前調用的方法。

#### 實作案例
Blog 網站地圖  

![blog sitemap](//images.ctfassets.net/64scck6nxm6l/ksCIK28r6iVmQvnGOOVO0/265362feceba66dda7b7c94039d23411/blog-sitemap.png)

以下為針對 HTML Head 的配置說明  
目前 blog 的預設配置在 nuxt.config.js，有可能被覆寫 meta 設定加上 hid 屬性  

``` javascript
// nuxt.config.js
export default {
  head: {
    title: '被程式設計的猴子',
    meta: [
      { 'http-equiv': 'Content-Type', content: 'text/html; charset=UTF-8' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' },
      { name: 'author', content: 'MonkeyBinBin' },
      { property: 'fb:app_id', content: config.fbId },
      { hid: 'keywords', name: 'keywords', content: '被程式設計的猴子, Nuxt.js, Bootstrap 4, MonkeyBinBin, blog' },
      { hid: 'description', name: 'description', content: '使用 Nuxt.js、 Bootstrap 4 建立的blog。分享與紀錄一些程式開發的東西。' },
      { hid: 'og:title', property: 'og:title', content: '被程式設計的猴子' },
      { hid: 'og:type', property: 'og:type', content: 'article' },
      { hid: 'og:url', property: 'og:url', content: path.join(config.domain, baseUrl) },
      { hid: 'og:image', property: 'og:image', content: path.join(config.domain, baseUrl, '/img/fb.jpg') },
      { hid: 'og:image:width', property: 'og:image:width', content: '474' },
      { hid: 'og:image:height', property: 'og:image:height', content: '474' },
      { hid: 'og:description', property: 'og:description', content: '使用 Nuxt.js、 Bootstrap 4 建立的blog。分享與紀錄一些程式開發的東西。' }
    ],
    link: [
      { rel: 'icon', type: 'image/x-icon', href: `${baseUrl}favicon.ico` }
    ]
  }
}
```

若其他頁面有需要更改預設的 meta 標籤，就在子組件使用"hid"屬性來設定覆蓋。  
this.post 為 data 內的資料，從 api 取得資料後會存放於此。  
增加 script 設定是為了可以在文章內崁入 codepen。

以下為文章詳細內容頁(pages/article/_id.vue) head 部份程式碼

``` javascript
// /pages/article/_id.vue
export default {
  name: 'Article',
  head () {
    const _head = {
      title: this.errorMsg || this.post.title,
      meta: [
        { hid: 'og:url', property: 'og:url', content: `${constant.domain}${constant.baseUrl}article/${this.id}/` }
      ],
      script: [
        { src: '//assets.codepen.io/assets/embed/ei.js' }
      ]
    }
    if (this.post && this.post.tags) {
      _head.meta.push({ hid: 'keywords', name: 'keywords', content: this.post.tags.join() })
    }
    if (this.post && this.post.slug) {
      _head.meta.push({ hid: 'description', property: 'description', content: this.post.slug })
      _head.meta.push({ hid: 'og:description', property: 'og:description', content: this.post.slug })
    }
    if (this.post && this.post.title) {
      _head.meta.push({ hid: 'og:title', property: 'og:title', content: this.post.title })
    }
    return _head
  }
}
```

以下為2篇不同頁面的結果  
比較2張圖片右側開發者工具呈現的 meta 資訊就可以看出差異  
主要是為了 seo 與 fb 分享可以針對文章的內容取得更精準的資訊

![meta sample 01](//images.ctfassets.net/64scck6nxm6l/4juMkzLIVquNxgWSGHaeCi/068852f85c9d745fc2d7df8d51842883/meta-sample-01.png)

![meta sample 02](//images.ctfassets.net/64scck6nxm6l/78texZmvVmGlCDFQVW66mH/80caef7ad831cd2595ed39b5be3438e3/meta-sample-02.png)

使用首頁來解析一下 Layout 切分方式  
1. 所有頁面都存在 Header 與 Footer，所以將這2個組件都放置於 Layout。
2. 置入 <nuxt/\> 讓切換路由時在此區塊渲染對應 pages 畫面。  

![blog layout](//images.ctfassets.net/64scck6nxm6l/3ekcA8cJYxW8Wb8fpB9A6r/c392acd8ca8b041d2d59f2361e11070f/layout.jpg)

layouts/default.vue 部份程式碼，可看到 template 中 HTML 排列方式

``` javascript
<template>
  <div>
    <page-header/>
    <div class="container">
      <nuxt/>
    </div>
    <page-footer/>
  </div>
</template>

<script>
import PageHeader from '~/components/PageHeader'
import PageFooter from '~/components/PageFooter'
export default {
  name: 'Pages',
  components: {
    PageHeader,
    PageFooter
  },
  // ...略
}
</script>
```

客製錯誤頁面 layouts/error.vue  

![blog error](//images.ctfassets.net/64scck6nxm6l/6wwlkExFGMVEBIoselLqHt/953cdaacedb45ceb8f17a9aeef460c96/error.jpg)

layouts/error.vue 部份程式碼  
透過 props 傳遞 error 參數，取得相關的錯誤資訊提供畫面顯示使用  

``` javascript
<template>
  <div class="row">
    <div class="col-12 text-center">
      <h1 class="not-found-title">{{error.statusCode}}</h1>
      <p class="text-black-50" v-if="error.statusCode === 404">Page not found</p>
      <p class="text-black-50" v-else>Oops, the page you're looking for doesn't exist.</p>
      <nuxt-link
        to="/"
        class="btn btn-dark"
      >回首頁</nuxt-link>
    </div>
  </div>
</template>

<script>
export default {
  name: 'Error',
  props: ['error'],
  // ...略
}
</script>
```

#### 參考資料
[Nuxt.js - Views](https://nuxtjs.org/guide/views)  
[Vue Meta](https://vue-meta.nuxtjs.org/)
]]></content:encoded>
            <category>Nuxt.js</category>
        </item>
        <item>
            <title><![CDATA[Nuxt 系列 - #3 搞懂路由怎麼搞]]></title>
            <link>https://monkeybinbin.github.io/article/a-series-of-nuxtjs-03</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/a-series-of-nuxtjs-03</guid>
            <pubDate>Tue, 14 Apr 2020 00:00:00 GMT</pubDate>
            <description><![CDATA[#### 關於路由
Nuxt.js 會依據在 **pages** 目錄內的 vue 檔案自動生成路由配置。目前有提供4種路由，分別為基礎路由、動態路由、嵌套路由與動態嵌套路由，接下來會分別說明4種路由的配置方式與規則。還有關於自訂換頁的特效與 middleware 的用途說明。  
另外在頁面之間要切換路由時，建議使用 \。目前 \ 與 \<router]]></description>
            <content:encoded><![CDATA[#### 關於路由
Nuxt.js 會依據在 **pages** 目錄內的 vue 檔案自動生成路由配置。目前有提供4種路由，分別為基礎路由、動態路由、嵌套路由與動態嵌套路由，接下來會分別說明4種路由的配置方式與規則。還有關於自訂換頁的特效與 middleware 的用途說明。  
另外在頁面之間要切換路由時，建議使用 \<nuxt-link>。目前 \<nuxt-link> 與 \<router-link> 用法一致，可至 [Vue Router documentation](https://router.vuejs.org/api/#router-link) 查閱所有資訊。

``` javascript
<template>
  <nuxt-link to="/">Home page</nuxt-link>
</template>
```

#### 基礎路由
以下檔案結構

pages/  
&emsp;∟ user/  
&emsp;&emsp;&emsp;∟ index.vue  
&emsp;&emsp;&emsp;∟ one.vue  
&emsp;∟ index.vue  

會自動產生以下路由設定  
.vue 檔案的數量會與路由產生的數量相同  

``` javascript
router: {
  routes: [
    {
      name: 'index',
      path: '/',
      component: 'pages/index.vue'
    },
    {
      name: 'user',
      path: '/user',
      component: 'pages/user/index.vue'
    },
    {
      name: 'user-one',
      path: '/user/one',
      component: 'pages/user/one.vue'
    }
  ]
}
```
這邊要特別說明的是 name 為 user-one 這個路由，可以發現這個 name 使用 **-** 符號來做分隔 user 與 one，這個是 nuxt 預設的設定。如果想要調整這個部份可至 nuxt.config.js 檔案中設定 [router.routeNameSplitter](https://nuxtjs.org/api/configuration-router#routenamesplitter)。 

#### 動態路由
使用"**下底線**"定義 .vue 檔案名稱或資料夾名稱，就會被視為使用參數的動態路由。

以下檔案結構

pages/  
&emsp;∟ _slug/  
&emsp;&emsp;&emsp;∟ comments.vue  
&emsp;&emsp;&emsp;∟ index.vue  
&emsp;∟ users/  
&emsp;&emsp;&emsp;∟ _id.vue  
&emsp;∟ index.vue  

會自動產生以下路由設定

``` javascript
router: {
  routes: [
    {
      name: 'index',
      path: '/',
      component: 'pages/index.vue'
    },
    {
      name: 'users-id',
      path: '/users/:id?',
      component: 'pages/users/_id.vue'
    },
    {
      name: 'slug',
      path: '/:slug',
      component: 'pages/_slug/index.vue'
    },
    {
      name: 'slug-comments',
      path: '/:slug/comments',
      component: 'pages/_slug/comments.vue'
    }
  ]
}
```

請注意 name 為 users-id 的路由 path 帶有 :id? 參數，代表 id 這個參數為非必要的，所以當你的 path 為 /users 時，也會符合 users-id 這個路由設定，此時 id 的值將會是 undefined。  
如果想讓 id 參數為必要的，可於 pages/users/_id.vue 增加 validate 方法來檢核參數值。當方法 return 不為 true、Promise 中 resolve 不為 true 或發生錯誤，都會自動顯示 404 或 500 錯誤頁面。

``` javascript
export default {
  validate ({ params }) {
    // Must be a number
    return /^\d+$/.test(params.id)
  }
}
```

更多關於路由參數檢核的資訊，請參考 [API Pages validate](https://nuxtjs.org/api/pages-validate)

#### 嵌套路由
建立嵌套路由需要建立一個 .vue 檔案，同時建立一個與 .vue 檔案名稱相同的目錄，裡面存放被嵌套的子組件。並且在一開始建立的 .vue 檔案中增加 \<nuxt-child/> 來顯示子組件的內容。

以下檔案結構

pages/  
&emsp;∟ users/  
&emsp;&emsp;&emsp;∟ _id.vue  
&emsp;&emsp;&emsp;∟ index.vue  
&emsp;∟ users.vue  

會自動產生以下路由設定

``` javascript
router: {
  routes: [
    {
      path: '/users',
      component: 'pages/users.vue',
      children: [
        {
          path: '',
          component: 'pages/users/index.vue',
          name: 'users'
        },
        {
          path: ':id',
          component: 'pages/users/_id.vue',
          name: 'users-id'
        }
      ]
    }
  ]
}
```

於 pages/users.vue 中增加 \<nuxt-child/>

``` javascript
<template>
  <div class="container">
    <h1>pages/users.vue file</h1>
    <nuxt-child/>
  </div>
</template>
```

#### 動態嵌套路由
比較少見的應用場景，設定上相對複雜，但規則基本上就是依照前面所說的動態路由搭配嵌套路由去設定。以下為官方範例，供大家參考。

以下檔案結構

pages/  
&emsp;∟ _category/  
&emsp;&emsp;&emsp;∟ _subCategory/  
&emsp;&emsp;&emsp;&emsp;&emsp;∟ _id.vue  
&emsp;&emsp;&emsp;&emsp;&emsp;∟ index.vue  
&emsp;&emsp;&emsp;∟ _subCategory.vue  
&emsp;&emsp;&emsp;∟ index.vue  
&emsp;∟ _category.vue  
&emsp;∟ index.vue  

會自動產生以下路由設定

``` javascript
router: {
  routes: [
    {
      path: '/',
      component: 'pages/index.vue',
      name: 'index'
    },
    {
      path: '/:category',
      component: 'pages/_category.vue',
      children: [
        {
          path: '',
          component: 'pages/_category/index.vue',
          name: 'category'
        },
        {
          path: ':subCategory',
          component: 'pages/_category/_subCategory.vue',
          children: [
            {
              path: '',
              component: 'pages/_category/_subCategory/index.vue',
              name: 'category-subCategory'
            },
            {
              path: ':id',
              component: 'pages/_category/_subCategory/_id.vue',
              name: 'category-subCategory-id'
            }
          ]
        }
      ]
    }
  ]
}
```

#### 轉場特效
使用 Vue.js 的 [\<transition>](http://vuejs.org/v2/guide/transitions.html#Transitioning-Single-Elements-Components) 組件實現路由切換時的轉場特效。

Nuxt.js 預設的 transition 名稱為 page  
所以要讓每次切換路由時都有特定的轉場特效，就要在**全域的樣式檔案**中增加下列樣式，這邊使用淡入、淡出效果為範例。

``` css
.page-enter-active, .page-leave-active {
  transition: opacity .5s;
}
.page-enter, .page-leave-active {
  opacity: 0;
}
```

如果想要指定特定頁面的轉場效果的話，就在頁面組件中設定 transition 屬性值為 page 以外的名稱，並在全域樣式檔案中加上對應的樣式即可。

頁面組件設定

``` javascript
export default {
  transition: 'special'
}
```

增加全域樣式設定

``` css
.special-enter-active, .special-leave-active {
  transition: opacity .3s;
}
.special-enter, .special-leave-active {
  opacity: 0.3;
}
```

#### Middleware
在一個頁面或是一組頁面渲染之前執行一個自訂的函式(function)，Nuxt.js規定此類的程式應放置於 middleware/ 的目錄內。  
Middleware 函式傳入 [the context](https://nuxtjs.org/api/context) 為參數，並且可以是非同步的方法。

建立 middleware/stats.js

``` javascript
import axios from 'axios'

export default function ({ route }) {
  return axios.post('http://my-stats-api.com', {
    url: route.fullPath
  })
}
```

可將 middleware 設定於 nuxt.config.js，使用 router.middleware

``` javascript
export default {
  router: {
    middleware: ['stats']
    // or
    middleware: 'stats'
  }
}
```

或者設定於 page/index.vue 與 layouts/default.vue

``` javascript
export default {
  middleware: ['stats']
  // or
  middleware: 'state'
}
```

#### 參考資料
[Nuxt.js - Routing](https://nuxtjs.org/guide/routing)  
[Vue Router documentation](https://router.vuejs.org/api/#router-link)
]]></content:encoded>
            <category>Nuxt.js</category>
        </item>
        <item>
            <title><![CDATA[Nuxt 系列 - #2 關於 nuxt.config.js 的設定]]></title>
            <link>https://monkeybinbin.github.io/article/a-series-of-nuxtjs-02</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/a-series-of-nuxtjs-02</guid>
            <pubDate>Fri, 10 Apr 2020 00:00:00 GMT</pubDate>
            <description><![CDATA[#### build
在建立網頁應用程式時，依據需要自定義 Webpack 配置。以下為目前有使用到的設定屬性。  

**analyze** - 用來啟用 [webpack-bundle-analyzer](https://github.com/webpack-contrib/webpack-bundle-analyzer) 套件設定，分析打包後的 js 檔案內各個 module 佔的大小。當需]]></description>
            <content:encoded><![CDATA[#### build
在建立網頁應用程式時，依據需要自定義 Webpack 配置。以下為目前有使用到的設定屬性。  

**analyze** - 用來啟用 [webpack-bundle-analyzer](https://github.com/webpack-contrib/webpack-bundle-analyzer) 套件設定，分析打包後的 js 檔案內各個 module 佔的大小。當需要調校效能或觀察載入套件的檔案大小時很好用。  
* Type: Boolean or Object
* Default: false

``` javascript
export default {
  build: {
    analyze: true,
    // or
    analyze: {
      analyzerMode: 'static'
    }
  }
}
```

**extend** - 擴充 webpack 設定，可能是增加 loader 或是增加其他 webpack plugins 的設定。  
* Type: Function  

依據官方的說法，此方法會被呼叫2次，一次是 server bundle，另一次是 client bundle  
此方法會傳入2個參數，第一個為 Webpack config object，第二個為包含 isDev、isClient、isServer、loaders 屬性的 object  
透過 isDev、isClient、isServer 來判斷在什麼狀況下要做什麼 webpack 設定的調整  
``` javascript
export default {
  build: {
    extend (config, { isClient, isServer, isDev, loaders: { vue } }) {
      // Extend only webpack config for development and client-bundle
      if (isDev && isClient) {
        config.module.rules.push({
          enforce: 'pre',
          test: /\.(js|vue)$/,
          loader: 'eslint-loader',
          exclude: /(node_modules)/
        })
      }

      // Extend only webpack config for client-bundle
      if (isClient) {
        vue.transformAssetUrls.video = ['src', 'poster']
      }

      config.plugins.push(
        new webpack.ProvidePlugin({
          '$': 'jquery',
          '_': 'lodash',
          'moment': 'moment'
        })
      )
    }
  }
}
```

**extractCSS** - 設定是否使用 [extract-css-chunks-webpack-plugin](https://github.com/faceyspacey/extract-css-chunks-webpack-plugin)，用來將 css 提取至單獨文件，通常一個組件一個檔案，與 js 檔分開載入增加效能。  
* Type: Boolean  
* Default: false

建議在開發期間設定為 false，才會有熱更新的效果  
利用 process.env.DEPLOY_ENV 透過 package.json 中的 scripts 設定傳入值，以達到不同環境使用不同設定的需求  
``` javascript
export default {
  build: {
    extractCSS: process.env.DEPLOY_ENV === 'production'
  }
}
```

**optimization** - webpack 優化相關設定。  
* Type: Object  
* Default: 
``` javascript
{
  minimize: false,
  minimizer: undefined,
  splitChunks: {
    chunks: 'all',
    automaticNameDelimiter: '.',
    name: undefined,
    cacheGroups: {
      commons: {}
    }
  }
}
```

調整為開發時不做最小化壓縮，但打包正式環境版本時則要做最小化壓縮  
利用 process.env.DEPLOY_ENV 透過 package.json 中的 scripts 設定傳入值，以達到不同環境使用不同設定的需求  
``` javascript
export default {
  optimization: {
    minimize: process.env.DEPLOY_ENV === 'production'
  }
}
```  

#### css
設定全域 css 檔案來源，如果要使用 sass，專案需安裝 node-sass 與 sass-loader 套件。  
``` javascript
export default {
  css: [
    // 載入fontawesome樣式
    '@fortawesome/fontawesome-svg-core/styles.css',
    // 載入bootstrap
    'bootstrap/scss/bootstrap.scss',
    'bootstrap-vue/dist/bootstrap-vue.css',
    // 載入highlight.js樣式
    'highlight.js/styles/zenburn.css',
    // 載入aos樣式
    'aos/src/sass/aos.scss',
    // 主要css樣式(自訂的樣式)
    '~/assets/sass/main.scss'
  ]
}
```

#### env
配置在客戶端和伺服器端共享的環境變數。通過 process.env.baseUrl 或 context.env.baseUrl 來使用 baseUrl 環境變數。
* Type: Object  

``` javascript
export default {
  env: {
    baseUrl: '/',
    title: '被程式設計的猴子'
  }
}
```  

> 注意事項：  
> Nuxt 使用 webpack definePlugin 定義環境變數。  

#### generate
與靜態化打包時(npm run generate)相關的設定，動態路由產生靜態 html 之設定、發現未知路由時的錯誤頁面之設定…等等。

**fallback** - 設置錯誤頁面，當 nuxt 呈現未知路由時，會轉至此文件。  
* Type: String or Boolean  
* Default: 200.html  

當未設定或設定為 false，錯誤頁面的檔名為 200.html  
設置為 true，錯誤頁面的檔名為 404.html  
設置為字串，錯誤頁面的檔名就會是此設置的字串  
> 注意事項：  
> 官方建議使用 npm run generate 方式使用 nuxt 時，應設置為 true，使用 404.html 為檔案名稱。
``` javascript
export default {
  generate: {
    fallback: true
  }
}
```  

**routes** - nuxt 執行 generate 指令時，動態路由會被忽略，所以這邊要去指定所有想要輸出靜態化 html 的路由。
* Type: Array  

使用一個返回 Promise 對象的 function  
``` javascript
import axios from 'axios'

export default {
  generate: {
    routes () {
      return axios.get('https://my-api/users')
        .then((res) => {
          return res.data.map((user) => {
            return '/users/' + user.id
          })
        })
    }
  }
}
```

使用一個 callback(err, params) 的 function
``` javascript
import axios from 'axios'

export default {
  generate: {
    routes (callback) {
      axios.get('https://my-api/users')
        .then((res) => {
          const routes = res.data.map((user) => {
            return '/users/' + user.id
          })
          callback(null, routes)
        })
        .catch(callback)
    }
  }
}
```

使用 payload 加速動態路由生成  
nuxt.config.js 設置回傳路由與 payload   
``` javascript
import axios from 'axios'

export default {
  generate: {
    routes () {
      return axios.get('https://my-api/users')
        .then((res) => {
          return res.data.map((user) => {
            return {
              route: '/users/' + user.id,
              payload: user
            }
          })
        })
    }
  }
}
```  
/users/_id.vue 取得 payload 供程式在生成靜態 html 使用，所以要判斷 payload 是否有值  
``` javascript
async asyncData ({ params, error, payload }) {
  if (payload) return { user: payload }
  else return { user: await backend.fetchUser(params.id) }
}
```

#### head
定義網頁 head tags，相關資訊可參考 [vue-meta documentation](https://vue-meta.nuxtjs.org/api/#metainfo-properties)。例如：script、link、meta、title，這邊為通用的設定也就是所有頁面都會生效，另外也可在 pages 組件中定義各別的 head 設定。  
* Type: Object or Function  

``` javascript
export default {
  head: {
    title: config.title,
    meta: [
      { 'http-equiv': 'Content-Type', content: 'text/html; charset=UTF-8' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' },
      { name: 'author', content: 'MonkeyBinBin' },
      { property: 'fb:app_id', content: '' },
      { hid: 'keywords', name: 'keywords', content: 'demo,demo1,demo2' },
      { hid: 'description', name: 'description', content: 'description' },
      { hid: 'og:title', property: 'og:title', content: 'document title' },
      { hid: 'og:type', property: 'og:type', content: 'article' },
      { hid: 'og:url', property: 'og:url', content: 'https://monkeybinbin.github.io/' },
      { hid: 'og:image', property: 'og:image', content: 'https://monkeybinbin.github.io/img/fb.jpg' },
      { hid: 'og:image:width', property: 'og:image:width', content: '474' },
      { hid: 'og:image:height', property: 'og:image:height', content: '474' },
      { hid: 'og:description', property: 'og:description', content: 'description' }
    ],
    link: [
      { rel: 'icon', type: 'image/x-icon', href: `/favicon.ico` }
    ]
  }
}
```

#### loading
可設定載入進度效果，有三種選擇分別為關閉、啟用預設效果並調整效果或自訂載入效果組件。
* Type: Boolean or Object or String  

如果有開啟，在路由變換時會觸發此效果。  
可利用 this.$nuxt.$loading.start() 與 this.$nuxt.$loading.finish() 來觸發出現此效果。  
``` javascript
export default {
  mounted () {
    this.$nextTick(() => {
      this.$nuxt.$loading.start()

      setTimeout(() => this.$nuxt.$loading.finish(), 500)
    })
  }
}
```

關閉載入進度效果  
``` javascript
export default {
  loading: false
}
```

使用內建載入進度效果，並且可自訂部份設定  
可配置項目與說明  

屬性名稱|類型|預設值|說明
-----------|----|---|----------------------
color|String|\'black\'|進度條顏色
failedColor|String|\'red\'|頁面加載失敗時的顏色(data 或 fetch 方法返回錯誤時)
height|String|\'2px\'|進度條高度
throttle|Number|200|以毫秒為單位，顯示進度條之前等待的時間，防止閃爍
duration|Number|5000|以毫秒為單位，進度條最大的顯示時間
continuous|Boolean|false|當加載時間超過 duration，繼續顯示進度條的動態效果
css|Boolean|true|是否套用進度條預設樣式
rtl|Boolean|false|進度條動態效果為由右到左

``` javascript
export default {
  loading: {
    color: 'blue',
    height: '5px'
  }
}
```

自定義載入進度效果組件  
可新建一個組件替代預設載入進度效果組件，只需設定組件路徑  
自定義組件需實現以下方法  

方法|是否必須|說明
-------|-------|--------------
start()|是|觸發啟動進度條時呼叫，在此方法內顯示組件
finish()|是|觸發關閉進度條時呼叫，在此方法內隱藏組件
fail(error)|否|發生錯誤時呼叫
increase(num)|否|在載入過程中呼叫，num 為小於100的整數

在 components 目錄下建立自定義載入效果組件，例如 components/loading/index.vue
``` javascript
<template lang="html">
  <div class="loading-page" v-if="loading">
    <p>Loading...</p>
  </div>
</template>

<script>
export default {
  data: () => ({
    loading: false
  }),
  methods: {
    start () {
      this.loading = true
    },
    finish () {
      this.loading = false
    }
  }
}
</script>

<style scoped>
.loading-page {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(255, 255, 255, 0.8);
  text-align: center;
  padding-top: 200px;
  font-size: 30px;
  font-family: sans-serif;
}
</style>
```
更新 nuxt.config.js 設定  
``` javascript
export default {
  loading: '~/components/loading/index.vue'
}
```

#### mode
設定應用程式模式是否要使用 server side rendering。
* Type: String  
* Default: universal

可設定值：  
* spa：一般的單頁應用程式，不使用 server side rendering
* universal：server side rendering 搭配 client side navigation 一起使用  

``` javascript
export default {
  mode: 'universal'
}
```

#### modules
設定要使用的 nuxt modules，不同的 module 會有不同的設定參數，因此詳細使用方式就參照各個 module 的使用說明。
``` javascript
export default {
  modules: [
    '@nuxtjs/sitemap',
    ['@nuxtjs/google-tag-manager', { id: 'GTM-N24F89P' }],
    // global 載入sass的資源(variables、functions、mixins…)使用的套件相關設定在 styleResources
    '@nuxtjs/style-resources',
    ['nuxt-fontawesome', {
      component: 'fa',
      imports: [
        {
          set: '@fortawesome/free-solid-svg-icons',
          icons: ['fas']
        }
      ]
    }]
  ]
}
```

#### plugins
設定實例化 vue 需要載入的 Javascript 插件，每當你需要使用 Vue.use()時，就應該在 plugins 增加程式碼與設定。
* Type: Array
* 陣列元素 Type: String or Object

陣列元素 Type 為 String，直接設置 plugins 的程式路徑  
陣列元素 Type 為 Object 具備以下屬性  
* src: String(程式路徑)
* mode: String(值可以為 client 或 server)，用來定義此 plugins 在 client 或 server 使用。  

``` javascript
export default {
  plugins: [
    '~/plugins/bootstrap-vue.js',
    '~/plugins/filters.js',
    '~/plugins/disqus.js',
    { src: '~/plugins/scrollspy.js', mode: 'client' },
    '~/plugins/font-awesome.js'
  ]
}
```

#### router
自定義路由(vue-router)相關設定。

**base** - 配置應用程式根目錄。  
* Type: String  
* Default: \'/\'

``` javascript
export default {
  router: {
    base: '/app/'
  }
}
```

**scrollBehavior** - 配置當頁面跳轉至目標頁面渲染後都會呼叫此方法。所以可以用來設定換頁後頁面 scroll 的位置。
* Type: Function  

指定換頁後 scroll 位置捲動至最上方  
``` javascript
export default {
  router: {
    // 換頁將網頁捲至最上方
    scrollBehavior: function (to, from, savedPosition) {
      return { x: 0, y: 0 }
    }
  }
}
```

#### 非同步配置
由於設定檔內有一些項目設定需要由 api 取得相關資料，所以必需另外在 nuxt.config.js 內 import 套件呼叫 api 來取得資料。  
``` Javascript
/* 
axios-module cannot be used in nuxt.config.js
You need to import axios and configure it again
*/
import axios from 'axios'

export default async () => {
  const data = await axios.get('endpoint')
  return {
    head: {
      title: data.head.title
    }
  }
}
```

#### 參考資料
[Nuxt.js - Configuration](https://nuxtjs.org/guide/configuration)  
[webpack](https://webpack.js.org/)
]]></content:encoded>
            <category>Nuxt.js</category>
        </item>
        <item>
            <title><![CDATA[Nuxt 系列 - #1 介紹、建專案與資料夾配置]]></title>
            <link>https://monkeybinbin.github.io/article/a-series-of-nuxtjs-01</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/a-series-of-nuxtjs-01</guid>
            <pubDate>Sun, 05 Apr 2020 00:00:00 GMT</pubDate>
            <description><![CDATA[#### 什麼是 Nuxt.js?
一個基於 vue 的應用框架，支援不同的渲染方式(SSR、CSR 或靜態)，可輕鬆串接 REST API 或 GraphQL，並且也容易整合常用的 CMS 與 CSS 框架。此外，也定義了基本的專案目錄結、支援各種套件、可調改 webpack 的設定，保持一定的規範並且也有擴充的可能。  
目前我的 blog 網站透過 Nuxt.js 靜態化打包後(npm ru]]></description>
            <content:encoded><![CDATA[#### 什麼是 Nuxt.js?
一個基於 vue 的應用框架，支援不同的渲染方式(SSR、CSR 或靜態)，可輕鬆串接 REST API 或 GraphQL，並且也容易整合常用的 CMS 與 CSS 框架。此外，也定義了基本的專案目錄結、支援各種套件、可調改 webpack 的設定，保持一定的規範並且也有擴充的可能。  
目前我的 blog 網站透過 Nuxt.js 靜態化打包後(npm run generate)，部署於 GitHub Pages。文章內容則放置於 Contentful 進行內容管理並提供 API 服務。最後再利用 Travis CI 設定自動化佈署，當應用程式有修改上傳或者有新的內容發佈，就會自動更新網站內容。 

#### 特性
1. 基於 vue
2. 自動代碼分割(Code Splitting)
3. Server Side Rendering
4. 強大的路由系統，支援非同步資料
5. 靜態檔案服務
6. 支援 ES2015+ 語法
7. 打包壓縮 JS 與 CSS
8. 管理 head 元素(title、meta…等)
9. 本地開發支援熱更新
10. 集成 ESLint
11. 支援各種樣式預處理器(SASS、LESS、Stylus)
12. 支援 HTTP/2 推送
13. 擴展模塊化架構

#### 流程
從 Request 到 Render 的流程  
![nuxt schema](//images.ctfassets.net/64scck6nxm6l/2rnFr5ZaWdCQ8SA9Ijq9lG/6515f534388cee0d35c467c55c68072e/nuxt-schema.png)

#### 伺服器渲染(Universal SSR)
需執行於 nodejs 伺服器的環境

優點：
1. 更好的 SEO
2. 更快呈現頁面  

需注意的部份：
1. 部份第三方套件可能需要經過特殊處理才能使用
2. 佔用更多伺服器 CPU 資源

#### 單頁應用程式(SPA)
也可選擇使用 SPA 模式，提供強大的 SPA 部署機制，預設已處理好基本的打包、Code Splitting、壓縮，如有特別需求可再自行調整。

#### 靜態化(預渲染)
依據路由配置將指定的路由靜態化為對應的 HTML 檔案。這樣一來就可以將網站部署於任何一個靜態網站服務上，例如 Github Pages 或是 Netlify。目前 blog 所使用的也是這個方式，將網站部署於 Github Pages。

優點：
1. 提高性能
2. 提高 SEO
3. 減少網站托管成本

#### 開始使用
使用 [create-nuxt-app](https://github.com/nuxt/create-nuxt-app) 快速建立專案，目前 create-nuxt-app 版本為 2.15.0  
**建議 nodejs 版本為 10.x**  
**先確認已安裝 npx(npx 在 npm 版本 5.2.0 之後預設會一起安裝)**
```bash
npx create-nuxt-app <project-name>
```
過程中會詢問你幾個問題，可依照需要進行設定(可能會因為 create-nuxt-app 版本不同問題順序會略有不同)
1. Project name
2. Project description
3. Author name
4. Choose programming language => JavaScript、TypeScript
5. Choose the package manager => Yarn、Npm
6. Choose UI framework => None、Ant Design Vue、Bootstrap Vue、Buefy、Bulma、Element、Framevuerk、iView、Tachyons、Tailwind CSS、Vuesax、Vuetify.js
7. Choose custom server framework => AdonisJs、Express、Fastify、Feathers、hapi、Koa、Micro
8. Choose Nuxt.js modules(多選) => Axios、Progressive Web App (PWA) Support、DotEnv
9. Choose linting tools(多選) => ESLint、Prettier、Lint staged files、StyleLint
10. Choose test framework => None、Jest、AVA
11. Choose rendering mode => Universal (SSR)、Single Page App
12. Choose development tools(多選) => jsconfig.json(Recommended for VS Code)、Semantic Pull Requests

完成以上設定就會開始安裝相關的 packages  
我在測試建立專案時出現了一個錯誤訊息  
![create nuxt app error](//images.ctfassets.net/64scck6nxm6l/6bttOHnEXYwzxHSHUxOScY/e2717129384a99e5ea1472b1353612bd/create-nuxt-app-error.png)  
依據 [Failed to load plugin 'unicorn' #6963](https://github.com/nuxt/nuxt.js/issues/6963) 解決方法為將 nodejs 版本改為 10.x 即可解決此問題  
除了上述的方式之外也可自行從頭開始新建專案，但設定上就相對的要花費更多時間，建議還是使用上述方式建立之後再去做調整會比較快  

建立完成後，執行以下指令即可啟動網站供開發使用

``` bash
npm run dev
```

啟動完成後開啟瀏覽器輸入網址 http://localhost:3000 ，即可看到以下畫面表示啟動成功

![nuxt-default-page](//images.ctfassets.net/64scck6nxm6l/2pI6kwYfPkFij8nVf2HniZ/575914724c17027772eb15a5d93c1306/nuxt-default-page.png)

#### 專案目錄結構
**assets** - 未編譯的靜態資源檔案，例如樣式、圖片或字型檔案。  
**components** - 用於頁面的 Vue.js 組件，這些組件不會像 pages 組件那樣有 asyncData 方法。  
**layouts** - 用於設定應用程式的佈局。  
**middleware** - 自訂在進入 layouts 或是 pages 開始渲染的方法，例如頁面需要驗證登入狀態的方法。  
**pages** - 用於組織應用程式的路由與頁面。Nuxt.js 會依據此資料夾內的所有檔案自動生成對應的路由配置。  
**plugins** - 注入 vue instance 的插件，例如 UI framework、filter。  
**static** - 不需經過 webpack 編譯的靜態檔案，例如 robots.txt。  
**store** - 用於 Vuex 相關功能的配置。  
**nuxt.config.js** - Nuxt.js 設定檔，可依據需要自行調整。

#### 別名
~ 或 @ 表示為 srcDir 目錄  
~~ 或 @@ 表示為 rootDir 目錄  
(預設 srcDir 與 rootDir 相同)  
``` javascript
import Logo from '~/components/Logo'
import Logo from '@/compoments/Logo'
```

> 在 vue template 中，如果需要引入 assets 或 static 目錄內的檔案，使用 "\~/assets/image.png" 和 "\~/static/image.png"。

#### 參考資料
[Nuxt.js](https://nuxtjs.org/)  
]]></content:encoded>
            <category>Nuxt.js</category>
        </item>
        <item>
            <title><![CDATA[使用 BEM 與 SCSS 讓 css 不再眼花撩亂]]></title>
            <link>https://monkeybinbin.github.io/article/sass-with-bem</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/sass-with-bem</guid>
            <pubDate>Mon, 02 Sep 2019 00:00:00 GMT</pubDate>
            <description><![CDATA[程式開發的「命名」一直是大家討論與關注的重點。透過命名我們可以獲得那些訊息？什麼樣的命名是好的？透過一致性的命名規則讓程式碼易讀、易懂並且可重用，讓開發和維護更有效率。我認為要使用什麼樣的命名規則應該要依照需要與團隊習慣來決定。沒有什麼命名方式一定是好的，只要符合需要就是好的。當我們在開發網站時，整個網站做下來會有非常多的樣式設定，如何達到樣式的共用與結構化，一眼就能看出樣式的用途與義意是非常重要]]></description>
            <content:encoded><![CDATA[程式開發的「命名」一直是大家討論與關注的重點。透過命名我們可以獲得那些訊息？什麼樣的命名是好的？透過一致性的命名規則讓程式碼易讀、易懂並且可重用，讓開發和維護更有效率。我認為要使用什麼樣的命名規則應該要依照需要與團隊習慣來決定。沒有什麼命名方式一定是好的，只要符合需要就是好的。當我們在開發網站時，整個網站做下來會有非常多的樣式設定，如何達到樣式的共用與結構化，一眼就能看出樣式的用途與義意是非常重要的，這也是我為什麼會選擇 BEM 的原因之一。

#### 常見樣式命名規則 ####
1. OOCSS
2. SMACSS
3. BEM  

上述三種方式為目前比較常見的樣式命名規則，以下會做簡單的介紹。

#### OOCSS 簡介 ####
OOCSS(Object Oriented CSS)字面意思是物件導向的CSS，追求元件的重用，樣式名命比較抽象。

主要原則：  
1. Separate structure and skin(分離結構和主題)
2. Separate container and content(分離容器和內容)

#### SMACSS 簡介 ####
SMACSS(Scalable and Modular Architecture for CSS)，模組化可擴展架構，主要目標為更語義化的 html 與 css，並且降低對依賴特定的 html 結構。

主要原則：
1. Categorizing CSS Rules(為css分類)，Base(基礎)、Layout(佈局)、Module(模組)、State(狀態)、Theme(主題)
2. Naming Rules(命名規則)，考慮用命名來表現樣式的分類，例如使用 l- 或 layout- 的字首來表現 Layout 這個類別的樣式
3. Minimizing the Depth of Applicability(最小化適配深度)，盡可能使用短的、不限定 html 結構的選擇符

#### BEM 簡介 ####
BEM(Block, Element, Modifier)，透過 Block、Element、Modifier 來描述頁面，從樣式名稱就可以看到一些有用的資訊，使程式碼更易於維護。

Block 是頁面中獨立存在的區塊，可以在不同場合下重用。每個頁面都可以看做是多個 Block 組成。(圖片來源：https://en.bem.info/methodology/key-concepts/)  
![bem block](//images.ctfassets.net/64scck6nxm6l/16nTpd1tDUzpTQ5bG89pXA/4bee9c45fc84088a718fa94e87852298/bem_block.png)

Element 是構成 Block 的元素，只有在 Block 內才具有意義，是依賴於 Block 而存在。(圖片來源：https://en.bem.info/methodology/key-concepts/)  
![bem element](//images.ctfassets.net/64scck6nxm6l/70KlcGq2GUb7hVNEkJ0c44/88d490eaf95a197f49994e3b6abadc43/bem_element.png)

Modifier 是用來描述 Block 或 Element 的屬性或狀態。同一個 Block 或 Element 可以有多個 Modifier。(圖片來源：https://en.bem.info/methodology/key-concepts/)  
![bem modifier](//images.ctfassets.net/64scck6nxm6l/1QoPwAuWeQQMgmhSokchUG/196060ced7b705d1a9e5b49c732b0a28/bem_modifier.png)

命名方式區分為3種：  
1. suit - http://suitcss.github.io/  
```css
.BlockName {}
.BlockName--modifierName {}
.BlockName-elementName {}
.BlockName-elementName--modifierName {}
```  
2. inuit - https://csswizardry.com/2013/01/mindbemding-getting-your-head-round-bem-syntax/  
```css
.block-name {}
.block-name--modifier-name {}
.block-name__element-name {}
.block-name__element-name--modifier-name {}
```  
3. yandex - http://bem.info/  
```css
.block-name {}
.block-name_modifier_name {}
.block-name__element-name {}
.block-name__element-name_modifier_name {}
```  
目前我選用的是 inuit 的命名方式，我的感覺這個規則比較單純。modifier 前面固定加上 --，element 前面固定加上 __。  

主要原則：  
1. "不可"使用 css 標籤選擇器和 id 選擇器(例：h3, .demo li, #demo)
2. Block 不能影響所在環境，不應該設置任何會影響外部的 style(例：margin)  
3. Block 和 Block 之間可以不分層次的嵌套
4. Element 和 Element 之間可以不分層次的嵌套
5. Element 一定是屬於 Block 的一部份，這表示 Element "無法"定義層次結構(錯誤範例：~~block__element1__element2~~)
6. 如果某段樣式可能會重覆使用，且不依賴於其他組件時，就應該建立一個 Block
7. 如果某段樣式在沒有 Block 的情況下無法使用，就應該建立一個 Element
8. Modifier 用來描述 Block 或 Element 的外觀、狀態與行為。  

#### 為何我要選擇 BEM ？ ####
1. 從命名可看出樣式的用途，明顯能夠區分出樣式間的關聯
2. 命名規則明確，只針對區塊做樣式的設定，比較不會影響到區塊以外的樣式
3. 樣式名稱比較不容易有衝突(~~因為名稱很長，有人可能覺得這是缺點~~)，取名的時候不用想破頭
4. 避免出現太過複雜的選擇器(複雜的選擇器範例：.demo > li:not(.hello) > a ~ *.hi)，比較容易看的懂(這點其他2個命名規則應該也能達到)  

#### BEM 搭配 Sass ####
利用 Sass 的 Nesting 與 & 符號來簡化樣式的寫法  
不需要一直重覆寫 Block 與 Element 名稱  

使用 BEM 命名規則搭配 Scss 開發之樣式  
```Scss
// 頁面整體的 Layout
.wrapper {
  display: flex;
  height: 100%;
  // 左側選單
  &__menu {
    width: 200px;
  }
  // 右側主要內容
  &__content {
    background: white;
    width: 100%;
    padding: 10px;
  }
}

// 側邊選單的樣式
.side-menu {
  display: flex;
  flex-direction: column;
  background: #054b7c;
  color: #fbd300;
  // 選單內的 item 樣式
  &__item {
    padding: 10px 20px;
    background: #054b7c;
    cursor: pointer;
    transition: background linear .3s, border ease-in-out .2s;
    // 選單內的 item 被選中的樣式
    &--active{
      color:#054b7f;
      background: #fbd300;
      border-left: 8px #fc5801 solid;
    }
    // 選單內的 item 有滑鼠移至上方時的樣式
    &:hover {
      color:#054b7f;
      background: #fbd300;
      border-right: 8px #06F0A9 solid;
    }
  }
}
```  

轉譯為 css 後的結果(符合 BEM 規則)  
```css
.wrapper {
  display: flex;
  height: 100%;
}
.wrapper__menu {
  width: 200px;
}
.wrapper__content {
  background: white;
  width: 100%;
  padding: 10px;
}

.side-menu {
  display: flex;
  flex-direction: column;
  background: #054b7c;
  color: #fbd300;
}
.side-menu__item {
  padding: 10px 20px;
  background: #054b7c;
  cursor: pointer;
  transition: background linear .3s, border ease-in-out .2s;
}
.side-menu__item--active{
  color:#054b7f;
  background: #fbd300;
  border-left: 8px #fc5801 solid;
}
.side-menu__item:hover {
  color:#054b7f;
  background: #fbd300;
  border-right: 8px #06F0A9 solid;
}
```  

完整的程式碼範例在這裡哦！！
<iframe height="500" style="width: 100%;" scrolling="no" title="Side menu sample with bem" src="//codepen.io/WenpinLiao/embed/OJLxybE/?height=300&theme-id=11023&default-tab=css,result" frameborder="no" allowtransparency="true" allowfullscreen="true">
  See the Pen <a href='https://codepen.io/WenpinLiao/pen/OJLxybE/'>Side menu sample with bem</a> by MonkeyBinBin
  (<a href='https://codepen.io/WenpinLiao'>@WenpinLiao</a>) on <a href='https://codepen.io'>CodePen</a>.
</iframe>


#### 感想 ####
命名規則只是一個輔助的方式，盡可能符合。但如果有特殊情況可視狀況來做調整。工具是用來幫助自已不是用來困住自已的。]]></content:encoded>
            <category>BEM</category>
            <category>SCSS</category>
        </item>
        <item>
            <title><![CDATA[利用 shell script 快速產出前端專案交付上版檔案 ]]></title>
            <link>https://monkeybinbin.github.io/article/sh-build-frontend-and-zip</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/sh-build-frontend-and-zip</guid>
            <pubDate>Thu, 15 Aug 2019 00:00:00 GMT</pubDate>
            <description><![CDATA[當專案開發進入測試階段之後，每天都會上演的戲碼就是包版做程式的更新。為了每天可以準時下班，就必需讓包版自動化。一方面可以省時，也可以減少犯錯的可能。接下來，我使用 React 專案來做範例，此專案已設定好不同環境(uat、prod)的 scripts 透過 npm、webpack 打包程式，說明在 Mac OS 如何使用 shell script 來進行打包、壓縮加密、更新版本，產出應交付的檔案。]]></description>
            <content:encoded><![CDATA[當專案開發進入測試階段之後，每天都會上演的戲碼就是包版做程式的更新。為了每天可以準時下班，就必需讓包版自動化。一方面可以省時，也可以減少犯錯的可能。接下來，我使用 React 專案來做範例，此專案已設定好不同環境(uat、prod)的 scripts 透過 npm、webpack 打包程式，說明在 Mac OS 如何使用 shell script 來進行打包、壓縮加密、更新版本，產出應交付的檔案。

#### 需求說明 ####
1. 上版環境區分為 uat 與 prod。
2. 上版檔案需加密壓縮，密碼為"test123"。
3. 檔案名稱分別為 "xxx v0.0.0 uat.zip" 、 "xxx v0.0.0 prod.zip"，其中0.0.0為版號。
4. 產出交付 zip 檔後，版控也需要更新至最新版號。

#### 使用到的 shell script 指令 ####
1. read
2. echo
3. date
4. cd
5. sed
6. for...in
7. if
8. rm
9. mkdir
10. cp
11. expect、spawn、send

#### 如何執行 .sh 檔案？ ####
1. 開啟「終端機」，將資料夾移至 .sh 檔案所在的資料夾。
2. 直接輸入"sh filename.sh"。

#### 1. 建立 .sh 檔案。 ####
使用文字編輯器，增加以下內容，定義所要使用的 shell。

``` bash
#!/bin/bash
```

#### 2. 設定變數與要求使用者輸入資料。 ####
* 所有變數都視為字串。
* 定義變數時，等號二邊不可以有空白。
* date 為取得目前日期指令，後方 +"%Y%m%d%H%M" 用來格式化日期並輸出字串。
* 使用 $(date +"%Y%m%d%H%M") 或 \`date +"%Y%m%d%H%M"\` 取得執行後的結果字串(執行結果輸出範例：201908151235)。
* 使用變數時在前方加上 $ 符號(例：$TARGET_PATH)。
* 使用 read 指令來讀取使用者輸入的資料。  

``` bash
PROJECT_PATH='/Users/demo/Projects/react-app'
TARGET_PATH='/Users/demo/react-app'
BUILD_OUTPUT_FOLDER=$(date +"%Y%m%d%H%M")
BUILD_OUTPUT_PATH=$TARGET_PATH/$BUILD_OUTPUT_FOLDER
ZIP_PASSWORD='test123'

read -p '請輸入版本號:' version
```  

#### 3. 修改 react 專案檔案，更新版本號碼。 ####
* 先將目錄移至 react 專案目錄，方便修改專案檔案與執行打包指令。
* 使用 sed 指令修改專案 package.json 檔案內的 version 設定。
* sed 指令後方的 -i 為直接修改讀取的檔案內容，接續的 "" 用來設定備份檔案名稱的後綴(suffix)，如不需備份則設定空字串。
* sed 指令後方的 's/"version": "[0-9][0-9]*\.[0-9][0-9]*\.[0-9][0-9]*",/"version": "'"$version"'",/g' 是用來指定動作， s 表示取代，s/被取代的字串/取代的字串/g，被取代的字串可搭配正規表示法。
* sed 最後一個參數為要被修改的檔案。 
 
``` bash
cd $PROJECT_PATH
sed -i "" 's/"version": "[0-9][0-9]*\.[0-9][0-9]*\.[0-9][0-9]*",/"version": "'"$version"'",/g' package.json
```  

#### 4. 建立輸出檔案使用的資料夾。 ####
* 先判斷檔案輸出的資料夾是否存在，不存在才建立資料夾。
* 指令中如果有需要空白字串，需在空字串前增加 \ 符號。
* mkdir 建立資料夾，參數 -p 表示當父層資料夾不存在時一併建立。  

``` bash
if [ ! -d $BUILD_OUTPUT_PATH/xxx\ v$version\ uat ]
then
  mkdir -p $BUILD_OUTPUT_PATH/xxx\ v$version\ uat
fi
```  

#### 5. 執行 npm 指令，產出打包檔案。 ####
``` bash
npm run build-uat
```  

#### 6. 將打包產出的檔案複製到"步驟4"建立的資料夾，提供稍後加密壓縮使用。 ####
* 使用 cp 指令複製檔案至指定資料夾。
* 參數 -r 表示複製資料夾以及裡面全部內容。
* cp -r [from folder] [to folder]。  

``` bash
cp -r $PROJECT_PATH/build/* $BUILD_OUTPUT_PATH/xxx\ v$version\ uat/
```  

#### 7. 加密壓縮。 ####
* 先將目錄移至"步驟4"建立的資料夾。
* 使用 zip 指令進行壓縮，參數 -e 為加密壓縮，參數 -r 為壓縮資料夾以及裡面全部內容。
* 利用 expect 來處理 zip 過程中需要使用者輸入密碼的動作。
* spawn、send 為 expect 的指令，所以要在 bash 下執行需使用 expect -c "…" 這種特別的寫法，並且在雙引號內的雙引號前需加上 \。
* \r 表示按下 enter 送出的意思。
  
``` bash
cd $BUILD_OUTPUT_PATH
expect -c "
  spawn zip -er ./xxx\ v$version\ uat.zip ./xxx\ v$version\ uat/
  expect \"Enter password\"
  send \"$ZIP_PASSWORD\r\"
  expect \"Verify password\"
  send \"$ZIP_PASSWORD\r\"
  expect eof
"
cd $PROJECT_PATH
```  

#### 8. 增加 for 迴圈，重覆執行 4~7 的步驟，以符合打包多個環境之需求。 ####
* 設定環境變數，多個使用空白區隔。  

``` bash
ENV='uat prod'
for value in $ENV; do
  if [ ! -d $BUILD_OUTPUT_PATH/xxx\ v$version\ $value ]
  then
    mkdir -p $BUILD_OUTPUT_PATH/xxx\ v$version\ $value
  fi

  npm run build-$value

  cp -r $PROJECT_PATH/build/* $BUILD_OUTPUT_PATH/xxx\ v$version\ $value/

  cd $BUILD_OUTPUT_PATH
  expect -c "
    spawn zip -er ./xxx\ v$version\ $value.zip ./xxx\ v$version\ $value/
    expect \"Enter password\"
    send \"$ZIP_PASSWORD\r\"
    expect \"Verify password\"
    send \"$ZIP_PASSWORD\r\"
    expect eof
  "
  cd $PROJECT_PATH
done
```  

#### 9. 詢問是否需要更新版控。 ####
* 設定變數預設值的方式為， xxx=${xxx:-預設值}。  

``` bash
read -p "確定要更新版控的版本號碼至 $version (y/N)？" isUpdateGitVersion
isUpdateGitVersion=${isUpdateGitVersion:-N}
if [ $isUpdateGitVersion = 'Y' ] || [ $isUpdateGitVersion = 'y' ]; then 
  echo "Updating git..."
  git commit -a -m "release v$version"
  git push
  echo "The version control is updated to $version."
else
  echo "The version control is not updated."
fi
```  

#### 完整 .sh 內容 ####
增加一些資訊輸出，幫助了解目前 shell script 執行的狀況  

``` bash
#!/bin/bash
PROJECT_PATH='/Users/demo/Projects/react-app'
TARGET_PATH='/Users/demo/react-app'
BUILD_OUTPUT_FOLDER=$(date +"%Y%m%d%H%M")
BUILD_OUTPUT_PATH=$TARGET_PATH/$BUILD_OUTPUT_FOLDER
ZIP_PASSWORD='test123'

read -p '請輸入版本號:' version

echo "\n### 準備打包版號為：$version ###\n"
echo "專案路徑：$PROJECT_PATH"
echo "檔案輸出路徑：$BUILD_OUTPUT_PATH"
echo "壓縮密碼：$ZIP_PASSWORD"

cd $PROJECT_PATH
sed -i "" 's/"version": "[0-9][0-9]*\.[0-9][0-9]*\.[0-9][0-9]*",/"version": "'"$version"'",/g' package.json
echo "Project version is updated to $version"

ENV='uat prod'
for value in $ENV; do
  if [ ! -d $BUILD_OUTPUT_PATH/xxx\ v$version\ $value ]
  then
    echo "\nCreating [$BUILD_OUTPUT_PATH/xxx v$version $value] folder"
    mkdir -p $BUILD_OUTPUT_PATH/xxx\ v$version\ $value
    echo "\033[0;37;43m[$BUILD_OUTPUT_PATH/xxx v$version $value] folder created success!\033[0m"
  fi

  npm run build-$value
  echo "\033[0;37;43m$value build success!\033[0m\n"

  echo "Copying file..."
  echo "From => $PROJECT_PATH/build/*"
  echo "To => $BUILD_OUTPUT_PATH/xxx v$version $value/"
  cp -r $PROJECT_PATH/build/* $BUILD_OUTPUT_PATH/xxx\ v$version\ $value/
  echo "\033[0;37;43mCopy file to [$BUILD_OUTPUT_PATH/xxx v$version $value/] success!\033[0m\n"

  cd $BUILD_OUTPUT_PATH
  expect -c "
    spawn zip -er ./xxx\ v$version\ $value.zip ./xxx\ v$version\ $value/
    expect \"Enter password\"
    send \"$ZIP_PASSWORD\r\"
    expect \"Verify password\"
    send \"$ZIP_PASSWORD\r\"
    expect eof
  "
  cd $PROJECT_PATH
done

echo "\n\033[0;37;43mThe package is successful.\033[0m\n"
echo "Output => $BUILD_OUTPUT_PATH\n"

read -p "確定要更新版控的版本號碼至 $version (y/N)？" isUpdateGitVersion
isUpdateGitVersion=${isUpdateGitVersion:-N}
if [ $isUpdateGitVersion = 'Y' ] || [ $isUpdateGitVersion = 'y' ]; then 
  echo "Updating git..."
  git commit -a -m "release v$version"
  git push
  echo "The version control is updated to $version."
else
  echo "The version control is not updated."
fi
```  

#### 其他參考資料 ####
* [echo Colors and formatting](https://misc.flogisoft.com/bash/tip_colors_and_formatting)
* [sed 命令簡介](https://dywang.csie.cyut.edu.tw/dywang/linuxProgram/node42.html)  

~ END ~]]></content:encoded>
            <category>Shell script</category>
        </item>
        <item>
            <title><![CDATA[Mac 端終機大改造， iTerm2 + Oh my zsh 好看也好用]]></title>
            <link>https://monkeybinbin.github.io/article/mac-reform-itermapp</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/mac-reform-itermapp</guid>
            <pubDate>Mon, 26 Nov 2018 00:00:00 GMT</pubDate>
            <description><![CDATA[近來因為工作的關係接觸前端開發，需要大量的使用 command line 。又不想天天看著黑底白字的畫面工作，後來就找到了這個 iTerm2 ，可以自訂介面的顏色，當然還有很多其他的設定像是快速鍵…等等。過一陣子之後又發現 Oh my zsh 可以更強化終端機的功能。裝了之後就再也回不去了。  

#### 安裝 iTerm2
官方介紹：iTerm2 is a replacement for Te]]></description>
            <content:encoded><![CDATA[近來因為工作的關係接觸前端開發，需要大量的使用 command line 。又不想天天看著黑底白字的畫面工作，後來就找到了這個 iTerm2 ，可以自訂介面的顏色，當然還有很多其他的設定像是快速鍵…等等。過一陣子之後又發現 Oh my zsh 可以更強化終端機的功能。裝了之後就再也回不去了。  

#### 安裝 iTerm2
官方介紹：iTerm2 is a replacement for Terminal and the successor to iTerm. It works on Macs with macOS 10.10 or newer. iTerm2 brings the terminal into the modern age with features you never knew you always wanted.  

下載連結：https://www.iterm2.com/downloads.html  
下載後解壓縮檔案，再複製到「應用程式」資料夾  

#### 修改 iTerm2 介面配色
你可以自已動手修改，但可能要花很多時間。 [iTerm2-Color-Schemes](https://github.com/mbadolato/iTerm2-Color-Schemes) 有很多人家調好的配色，可以直接在這邊下載，請注意設定檔案的位置在「**schemes**」的資料夾內，副檔名為「**.itermcolors**」，下載完成後將檔案import至iTerm即可選用。  

#### 如何 import 設定檔( *.itermcolors )至 iTerm2
Preferences > Profiles > Colors > Color Presets > Import...  
請注意 import 並不會馬上生效，還要在 Color Presets 點選剛剛 import 的設定才會生效  
![iTerm Color Presets](//images.ctfassets.net/64scck6nxm6l/59hR8lxMokUIIANTFj1Pyk/10548c0aac14823eb7d8daf3d1111482/_00.jpg)  

![iTerm import color schema](//images.ctfassets.net/64scck6nxm6l/1Na6MWzYqUBjICNN0Y7VNh/2e12d868d3a66c6dc88c670124dbf8ab/_01.jpg)

目前我選用的配色是 **Brogrammer**  
![Brogrammer](//images.ctfassets.net/64scck6nxm6l/5i9wOzRuryGf5DnBsIPyTu/684c21d823ed637cea473d7e522a35c7/_02.jpg)

#### 安裝 zsh
透過 homebrew 安裝 zsh  
``` javascript
brew install zsh
```

#### 安裝 oh my zsh
官方介紹：Oh My Zsh is a delightful, open source, community-driven framework for managing your Zsh configuration. It comes bundled with a ton of helpful functions, helpers, plugins, themes, and a few things that make you shout...

用來管理 zsh 設定，提供大量有用的插件、主題…等等。  

安裝指令
``` javascript
$ sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
```

安裝完成後就可以修改 theme 設定    
修改方式方法如下  

1. 使用編輯器開啟 ~/.zshrc 檔案  
我習慣使用 vs code ,開啟檔案的指令如下(需有安裝 vs code,並且在PATH中安裝' code '指令)
``` javascript
code ~/.zshrc
```

2. 將 ZSH_THEME="robbyrussell" 的 robbyrussell 改為想要的 theme 名稱，設定完成儲存檔案  
內建的 theme 可以參考 [Link](https://github.com/robbyrussell/oh-my-zsh/wiki/themes)  

3. 套用 ~/.zshrc 設定，需執行以下指令才會生效
``` javascript
source ~/.zshrc
```  

推薦另外安裝 powerlevel9k 可以透過在 ~/.zshrc 設定自訂畫面顯示的資訊  
![powerlevel9k screen](//images.ctfassets.net/64scck6nxm6l/7sq2YNKhwcRIZWaFJXmwsi/76321e3929cfde278550d8369245da69/_03.jpg)

#### 安裝 zsh theme powerlevel9k
Powerlevel9k 不是 oh my zsh 內建的 theme ，需要另外下載。

1. 下載指令
``` javascript
git clone https://github.com/bhilburn/powerlevel9k.git ~/.oh-my-zsh/custom/themes/powerlevel9k
```

2. 使用編輯器開啟 ~/.zshrc 檔案  
``` javascript
code ~/.zshrc
```

3. 修改 ~/.zshrc 的 ZSH_THEME 設定為 powerlevel9k/powerlevel9k  
``` javascript
ZSH_THEME="powerlevel9k/powerlevel9k"
```

4. 修改 ~/.zshrc 增加 prompt 設定(項目以空格區隔)  
powerlevel9k 提供的prompt設定參考 [Link](https://github.com/bhilburn/powerlevel9k#prompt-customization)  
其他使用者提供的設定/截圖參考 [Link](https://github.com/bhilburn/powerlevel9k/wiki/Show-Off-Your-Config)  
目前我使用的設定如下
``` shell
POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(os_icon dir vcs)
POWERLEVEL9K_RIGHT_PROMPT_ELEMENTS=(status ip time node_version)
``` 

5. 修改 ~/.zshrc 增加字型設定，設定完成儲存檔案  
字型設定/說明 [Link](https://github.com/bhilburn/powerlevel9k/wiki/Install-Instructions#option-4-install-nerd-fonts)  
(請注意選擇不同的字型設定，需安裝該字型並將 iTerm2 之字型設定為該字型才會正常顯示)  
目前我使用的設定如下  
``` shell
POWERLEVEL9K_MODE='nerdfont-complete'
```  

6. 套用 ~/.zshrc 設定
``` javascript
source ~/.zshrc
```  

#### 安裝 nerd-fonts
[nerd-fonts github page](https://github.com/ryanoasis/nerd-fonts)  

透過 Homebrew 安裝字型說明 [Link](https://github.com/ryanoasis/nerd-fonts#option-4-homebrew-fonts)  
(font-hack-nerd-font 可替換為想要的字型名稱)  
``` shell
brew tap caskroom/fonts
brew cask install font-hack-nerd-font
```

搜尋 Homebrew 有那些 nerd-font 可安裝
``` shell
brew search nerd-font
```
![nerd-font search result](//images.ctfassets.net/64scck6nxm6l/cUvVl19gl9dg8NXhBfPZz/b317ebe4ab3777475ba57ca8cce38adc/_04.jpg)

#### 設定 iTerm2 字型
Preferences > Profiles > Text > Change Font  
將 Font、Non-ASCII Font 調整為剛剛安裝的 nerd 字型
![iTerm font setting](//images.ctfassets.net/64scck6nxm6l/uqhiLa9pWqurxccNLJfkO/1e5fe197daf4c40f3a11b20637176e1e/_05.jpg)

**請注意：如果設定字型後發現沒有作用，解決方法如下**  
**開啟font.app > 選擇該字體 > 選擇自動解決版本問題**  

#### oh my zsh plugin 安裝與設定
Plugins 設定/說明 [Link](https://github.com/robbyrussell/oh-my-zsh/wiki/Plugins)  
可依據需要安裝想要的plugin  
預設安裝的 plugins 資料夾位置在 ~/.oh-my-zsh/plugins  
透過下列指令可查詢目前預設可使用的 plugins  
``` shell
cd ~/.oh-my-zsh/plugins && ls
```
![oh my zsh default plugins](//images.ctfassets.net/64scck6nxm6l/6IM4W0f5N7ppZzm7LnNOQL/6401b8b813c72c177ee80a28d83c36ab/_06.jpg)

Plugins 設定方式一樣在~/.zshrc做設定，設定方式如下  
1. 使用編輯器開啟 ~/.zshrc 檔案  
``` shell
code ~/.zshrc
```

2. 設定/安裝 plugins (項目以空格區隔)，設定完成儲存檔案  
z 為預設的plugin不需額外安裝，功能是用來快速跳轉資料夾的指令  
zsh-syntax-highlighting 需額外安裝，功能是用來讓指令高亮呈現  
[zsh-syntax-highlighting 安裝說明(for oh my zsh)](https://github.com/zsh-users/zsh-syntax-highlighting/blob/master/INSTALL.md#oh-my-zsh)  
下載 zsh-syntax-highlighting 至 oh my zsh 資料夾之指令
``` shell
git clone https://github.com/zsh-users/zsh-syntax-highlighting.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting
```
目前我的 plugins 設定如下  
``` shell
plugins=(git z zsh-syntax-highlighting)
```

3. 套用 ~/.zshrc 設定
``` shell
source ~/.zshrc
```  

**z plugin**  
安裝套件後，只要曾經使用指令移至該資料夾之後就可以利用 z 指令快速移至該資料夾  
![z plugin](//images.ctfassets.net/64scck6nxm6l/3rUXMj4X81UNC5MYSIaTU4/31891d12cbbd06f35d4944b62b26657a/_07.jpg)

**zsh-syntax-highlighting plugin**  
輸入指令時，即時顯示高亮效果，可用指令與不可用的指令會有不同顏色標示  
![zsh-syntax-highlighting plugin](//images.ctfassets.net/64scck6nxm6l/OJbsA5Gpmkw95qr07dbxo/65956e1fe443bc0abbe4bc39483d14ce/_08.gif)  

#### iTerm 快速鍵設定
* 刪除整行輸入的指令  
> 自行定義快速鍵(目前使用 command + delete )，並將 Action 設定為 Send Hex Codes : **0x15**  
* 刪除輸入指令的一個單字  
> 自行定義快速鍵(目前使用 option + delete )，並將 Action 設定為 Send Hex Codes : **0x17**  

![hot keys setting](//images.ctfassets.net/64scck6nxm6l/629wVvn0dEYDuskYHTsms5/9d75a39b7d698fc9c86d3f9957696b90/_09.jpg)  

後續如果有什麼好用的東西再補充，暫時先這樣。  
]]></content:encoded>
            <category>iTerm2</category>
            <category>Oh my zsh</category>
        </item>
        <item>
            <title><![CDATA[Mac OS 利用 Automator 搭配 Ghostscript 壓縮 PDF]]></title>
            <link>https://monkeybinbin.github.io/article/mac-gs-compress-pdf</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/mac-gs-compress-pdf</guid>
            <pubDate>Tue, 25 Sep 2018 00:00:00 GMT</pubDate>
            <description><![CDATA[### 什麼是Ghostscript？
是一套建基於Adobe、PostScript及可移植文件格式（PDF）的頁面描述語言等而編譯成的自由軟體。

### 如何安裝Ghostscript？
1. 開啟"終端機"
2. 安裝(更新)Homebrew
``` sh
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/ins]]></description>
            <content:encoded><![CDATA[### 什麼是Ghostscript？
是一套建基於Adobe、PostScript及可移植文件格式（PDF）的頁面描述語言等而編譯成的自由軟體。

### 如何安裝Ghostscript？
1. 開啟"終端機"
2. 安裝(更新)Homebrew
``` sh
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)" < /dev/null 2> /dev/null
```
3. 安裝Ghostscript
``` sh
brew install ghostscript
```

### 如何使用Ghostscript壓縮PDF檔案？
安裝完Ghostscript之後，可利用指令gs來進行PDF檔案壓縮  
[Ghostscript command line 參考資料](https://www.ghostscript.com/doc/current/Use.htm#Help_command)  
壓縮PDF檔案的指令(參考)
``` sh
gs -sDEVICE=pdfwrite -dNOPAUSE -dQUIET -dBATCH -dPDFSETTINGS=/screen -dCompatibilityLevel=1.4 -sOutputFile=output.pdf input.pdf
```
執行指令時需注意**檔案路徑**的設定, 另外輸入、輸出的檔案不要使用**中文**檔名

### 什麼是Automator？
是蘋果公司為他們的Mac OS X系統開發的一款軟體。只要通過點擊拖拽滑鼠等操作就可以將一系列動作組合成一個工作流程，從而幫助你自動的（可重複的）完成一些複雜的工作。Automator還能橫跨很多不同種類的程序，包括：Finder、Safari、iCal或者其他的一些程序。它還能和一些第三方的程序一起工作。

### 如何利用Automator來執行Ghostscript壓縮PDF檔案？
1. 系統要安裝Ghostscript
2. 開啟Automator.app, 建立新的"服務"  
![automator start](//images.ctfassets.net/64scck6nxm6l/4UMEfwKQhAOtOkOQGngFA6/cb0e48e620dfc08683e27d3d0a6d3bfd/_01.png)
3. 參考下圖設定，並且新增"過瀘Finder項目"設定只允許pdf檔案進行轉換  
![automator finder setting](//images.ctfassets.net/64scck6nxm6l/4KCR6O5HigOjAauyrfZV3O/24e3ef4278bfba4607965b84d104d194/_02.png)
4. 新增"執行 Shell 工序指令"，將下方的shell script貼到指令區塊，並且將"傳遞輸入"改為"作為引數使用"  
``` sh
for f in "$@"
do
	INPUTFILE="$f"
	INPUTDIRNAME=$(dirname "$INPUTFILE")
	FINALFILENAME="${${INPUTFILE##*/}%.*}"
	RENAMEINPUTFILENAME="$INPUTDIRNAME"/"$(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1)".pdf
	OUTPUTFILENAME="$(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1)".pdf
	OUTPUTFILE="$INPUTDIRNAME"/"$OUTPUTFILENAME"
	FINALOUTFILE="$INPUTDIRNAME"/"$FINALFILENAME"_compressed.pdf

	cp "$INPUTFILE" "$RENAMEINPUTFILENAME"
	/usr/local/bin/gs -sDEVICE=pdfwrite -dNOPAUSE -dQUIET -dBATCH -dPDFSETTINGS=/screen -dCompatibilityLevel=1.4 -sOutputFile="$OUTPUTFILE" "$RENAMEINPUTFILENAME"
	rm -f "$FINALOUTFILE"
	mv "$OUTPUTFILE" "$FINALOUTFILE"
	rm -f "$RENAMEINPUTFILENAME"
	osascript -e 'tell app "System Events" to display dialog "PDF已產生!['$FINALOUTFILE']" default button 1 buttons {"OK"}'
done
```
設定完成畫面  
![automator shell script](//images.ctfassets.net/64scck6nxm6l/6EE6UDt6u5SInLKBMbIVMU/097505a61233bf93c356c167340c9a19/_03.png)
5. 設定完成後存檔即可使用，儲存的檔名即為在"服務"看到的名稱。在pdf檔案上按右鍵開啟功能選單，並且開啟"服務"即可看到該功能。按下後就會開始執行產生壓縮後的pdf檔案，依據上方提供的script產出的pdf檔案會是原檔名+"\_compressed"。(執行時間會因為pdf檔案的大小有所不同，請耐心等待)  
![automator exec](//images.ctfassets.net/64scck6nxm6l/5O6MnAHzTnMut0yKmEXzbV/09a6067b9ca774954c4971c2fd3150b2/_04.png)

### 如何自訂服務的快速鍵？
1. 開啟"系統偏好設定"進入"鍵盤"的設定，切換至"快速鍵"再選擇左側選單的"服務"。
![automator service hotkey](//images.ctfassets.net/64scck6nxm6l/6837b9GV3VHddLgOyHArL7/8f1900d4c34f62adfe648c5cf59a10c3/_05.png)
2. 找到前面服務存檔的名案名稱，選取該項目畫面就會出現"加入快速鍵"，按下即可設定快速鍵，設定完畢關閉視窗即可使用該快速鍵啟動服務。

### Demo
原本10.3MB的檔案壓縮後只剩下1.2MB
![demo](//images.ctfassets.net/64scck6nxm6l/YCPETtRoa4rYh73AS0aWW/1ff9d6f6080f47d0d54c047db67ad452/_06.gif)

\~ End \~
]]></content:encoded>
            <category>Automator</category>
            <category>Ghostscript</category>
            <category>PDF</category>
        </item>
        <item>
            <title><![CDATA[建立 Sass 開發與轉譯產出 CSS 環境配置]]></title>
            <link>https://monkeybinbin.github.io/article/set-sass-dev-environment</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/set-sass-dev-environment</guid>
            <pubDate>Sat, 08 Sep 2018 00:00:00 GMT</pubDate>
            <description><![CDATA[![sass logo](//images.ctfassets.net/64scck6nxm6l/5XprZe5a3HCsw7TjJFyQvd/91dee0cfc7660050db640237b0d57417/_01.png)

#### Sass是一種CSS擴展的語言。允許使用變數、嵌套(nesting)、混合(mixin)、導入(import)...等功能。並且可以完全兼容CSS語法。
###]]></description>
            <content:encoded><![CDATA[![sass logo](//images.ctfassets.net/64scck6nxm6l/5XprZe5a3HCsw7TjJFyQvd/91dee0cfc7660050db640237b0d57417/_01.png)

#### Sass是一種CSS擴展的語言。允許使用變數、嵌套(nesting)、混合(mixin)、導入(import)...等功能。並且可以完全兼容CSS語法。
#### Sass有兩種不同的語法，一個是Sass另一個是SCSS

Sass沒有大括號、分號 ，是一種indented syntax(縮進語法)，像這樣：
``` css
.block
    width: 960px
    height: 40px
    margin: 0 auto
    +mixin
    a
        color: blue
```

SCSS是一種接近CSS的語法，像這樣：
``` css
.block{
    width: 960px;
    height: 40px;
    margin: 0 auto;
    @include mixin
    a{
        color: blue;
    }
}
```
推薦使用SCSS語法，因為他更接近CSS語法容易閱讀與入門。
**附註：Sass的檔名如果在最前面加上"\_"就不會被轉譯工具輸出成CSS檔案(例如：\_base.scss)**

![compass logo](//images.ctfassets.net/64scck6nxm6l/24irDSoQ495a24xs7ABp2B/c8e7920b2191cc427e28e41e97dc467f/_02.png)

#### 一個open-source，且使用Sass開發的CSS框架，提供了很多便利的功能，例如撰寫css3時只要套用compass的mixin就會自動產出多個瀏覽器的前綴詞。
**請注意，目前compass已沒有在維護。**

SCSS sample code
``` css
@import "compass/css3/border-radius"

.demo{
    @include border-radius(5px);
}
```
產出的CSS code
``` css
.demo{
    -webkit-border-radius: 5px;
    -moz-border-radius: 5px;
    -ms-border-radius: 5px;
    -o-border-radius: 5px;
    border-radius: 5px;
}
```  

-------------

#### 環境需求
1.安裝**nodejs**與**npm**  
2.透過npm在global安裝**live-server**套件
``` javascript
npm install -g live-server
```
live-server是用在開發時啟動一個網站，即時查看樣式修改的結果，可依個人喜好選擇其他套件。  

#### 開發工具
Visual Studio Code  

-------------

#### 專案建立
1.新增專案資料夾，並且初始化專案npm設定。
``` javascript
mkdir <資料夾名稱>
```
``` javascript
cd <資料夾名稱>
```
``` javascript
npm init
```
![create project](//images.ctfassets.net/64scck6nxm6l/6SZLw4D61POT9OQsUqNuOX/25ee81ab91132a82a598d32c01db2a64/_03.png)

2.安裝node-sass套件。
``` javascript
npm i node-sass -D
```
![install node-sass](//images.ctfassets.net/64scck6nxm6l/2jMHyNPaFeGHi0cCRbC3Q6/d51e86cd118b60d8e592222572a99855/_04.png)

3.如果需要使用compass，安裝compass-importer。(如不需要可略過此步驟)
``` javascript
npm i compass-importer -D
```
![install compass-importer](//images.ctfassets.net/64scck6nxm6l/4JJ1vKdFeg5MhtsyByVfHh/805cb60b7d2be514299cf54e11902146/_05.png)

4.建立資料夾，以利後續專案的開發與維護。可依個人的需要做調整，只要注意在設定npm的scripts時也要做調整(指定Sass程式碼的資料夾與輸出CSS的資料夾)。  
參考資訊：[Architecture for a Sass Project](https://www.sitepoint.com/architecture-sass-project/)  
在專案根目錄下，新增index.html編寫html並且指定載入轉譯後的main.css檔案
``` html
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
  <link rel="stylesheet" href="css/main.css">
</head>

<body>
  <h1>Sass Demo Page</h1>
  <div class="demo">
    .demo block
  </div>
</body>

</html>
```
在sass目錄下，新增main.scss增加樣式設定(有使用compass)
``` css
@import "compass/css3/border-radius";

body {
  font-size: 14px;
  color: gray;
}

.demo{
  @include border-radius(10px);
  width: 200px;
  height: 100px;
  background-color: #9cf;
  color: black;
  padding: 10px;
}
```
![project folder](//images.ctfassets.net/64scck6nxm6l/3BMolHPP2yg42RvJmq7IP1/42e431b35b7f8a5172524ca5a63c4ad5/_06.png)

5.開啟package.json，增加scripts設定啟動live-server轉譯Sass，套件尚有其他進階設定請參考[live-server](https://github.com/tapio/live-server)與[node-sass](https://github.com/sass/node-sass)套件github網站，會有更詳細的介紹。  

**設定live-server的根目錄為./, 網站port為9999, 忽略build、sass資料夾內的檔案與json檔案的變更(檔案變更不重新reload)  
``` javascript
"start": "live-server ./ --port=9999 --ignore=build,sass,*.json"
```

**未使用compass的設定 => 設定監看sass資料夾內的檔案轉譯為CSS輸出至css資料夾，開啟source map
``` javascript
"sass:dev": "node-sass -w sass/ -o css/ --source-map=true"
```

**使用compass的設定 => 設定監看sass資料夾內的檔案轉譯為CSS輸出至css資料夾，開啟source map並且載入compass
``` javascript
"sass:dev": "node-sass -w sass/ -o css/ --source-map=true --importer=node_modules/compass-importer"
```
完成後基本的Sass開發環境也就完成，可以開始利用Sass來開發樣式了。  

6.如何啟動站台與轉譯Sass  
**各自開啟一個終端機視窗執行下列指令，啟動後2個終端機視窗都不要關閉**  
啟動站台(執行後會自動開啟網站http://127.0.0.1:9999)
``` javascript
npm run start
```
![server start](//images.ctfassets.net/64scck6nxm6l/2rp5u8L1TIi2bA80wNqCLS/7365883b5a3769850834654dac064ef6/_07.png)

啟動轉譯Sass
``` javascript
npm run sass:dev
```
![compile sass to css](//images.ctfassets.net/64scck6nxm6l/11yvtNDE1GYdG6yQwySSp0/bd16bc3d3b49c6662f9b3868ceb5713e/_08.png)

-------------

#### 如何輸出最小化的CSS檔案
建立script,在nodejs環境下進行Sass轉譯CSS並且修改輸出的檔案名稱
**撰寫script需使用的套件**
- **argv-parser**：解析指令傳入的參數。
- **glob**：自訂規則抓取檔案(名稱)
- **colors**：自訂輸出console時的文字、背景顏色

安裝指令
``` javascript
npm i argv-parser glob colors -D
```
**新增build資料夾，並且在build資料夾內新增index.js**  
加入程式碼
``` javascript
const sass = require('node-sass')
const glob = require('glob')
const fs = require('fs')
const path = require('path')
const colors = require('colors/safe')
const parser = require('argv-parser')

// 設定colors套件的顏色對應
colors.setTheme({
  silly: 'rainbow',
  input: 'grey',
  verbose: 'cyan',
  prompt: 'grey',
  info: 'green',
  data: 'grey',
  help: 'cyan',
  warn: 'yellow',
  debug: 'blue',
  error: 'red'
})

// 利用argv-parser套件解析傳入參數值
const data = parser.parse(process.argv, {
  rules: {
    settings: {
      minify: true,
      'source-map': true,
      'input-folder': '',
      'output-folder': '',
      compass: false
    }
  }
})
console.log(colors.verbose(`minify(是否最小化[預設為true]) => ${data.parsed.minify}`))
console.log(colors.verbose(`source-map(是否開啟source map[預設為true]) => ${data.parsed['source-map']}`))
console.log(colors.verbose(`*input-folder(Sass原始程式的資料夾[必填]) => ${data.parsed['input-folder']}`))
console.log(colors.verbose(`*output-folder(輸出css的資料夾[必填]) => ${data.parsed['output-folder']}`))
console.log(colors.verbose(`importer compass(是否載入compass[預設為false]) => ${data.parsed.compass}\n`))
console.log(colors.silly(sass.info) + `\n`)

// 取得npm scripts傳入的參數
const inputFolder = data.parsed['input-folder']
const outputFolder = data.parsed['output-folder']
const minify = data.parsed.minify
const sourceMap = data.parsed['source-map']
const compass = data.parsed.compass
const importer = compass ? require('compass-importer') : undefined

// 錯誤處理，必需指定Sass程式碼所在的資料夾與輸出css的資料夾
if (!(inputFolder && outputFolder)) {
  throw Error('Please setting input/output folder.(--input-folder, --output-folder)')
}

// 修改輸出檔案的路徑與檔名
function addMinExtension (fileName) {
  fileName = fileName.substr(0, fileName.lastIndexOf('.')) + (minify ? '.min.css' : '.css')
  // 修改路徑至輸出資料夾
  return fileName.replace(inputFolder, outputFolder)
}

// 確保寫入檔案時資料夾已存在，避免發生錯誤
function ensureDirectoryExistence (filePath) {
  var dirname = path.dirname(filePath)
  if (fs.existsSync(dirname)) {
    return true
  }
  ensureDirectoryExistence(dirname)
  fs.mkdirSync(dirname)
}

// 儲存檔案
function saveFile (dataToSave, filePath, message, callback) {
  fs.unlink(filePath, (err) => {
    if (err && err.code !== 'ENOENT') { throw err }

    ensureDirectoryExistence(filePath)

    let options = { flag: 'w' }
    fs.writeFile(filePath, dataToSave, options, (err) => {
      if (err) { console.log(colors.error(err)) } else {
        console.log(colors.info(message))
        callback && callback()
      }
    })
  })
}

// 使用glob套件取得所有要輸出的.scss檔案(sass資料夾內非_開頭的scss檔案))
const files = glob.sync(`${inputFolder}**/!(_*).scss`)
// 執行.scss檔案轉譯為.css
files.forEach(function (file) {
  const outputFileName = addMinExtension(file)
  // node-sass產出css的方法(參考node-sass的options說明)
  sass.render({
    file: file,
    outFile: outputFileName,
    sourceMap: sourceMap,
    outputStyle: minify ? 'compressed' : 'nested',
    importer: importer
  }, function (error, result) {
    if (error) {
      // 發生錯誤時的處理
      let errorToLog = error.formatted || error
      console.log(colors.error(errorToLog))
    } else {
      // 轉譯成功，產生.css檔案
      let compiledMessage = 'Saved compiled file ' + outputFileName
      saveFile(result.css.toString(), outputFileName, compiledMessage)

      if (result.map) {
        // 產生.map檔案
        let mapFileName = outputFileName + '.map'
        let savedMessage = 'Saved map file ' + mapFileName
        saveFile(result.map.toString(), mapFileName, savedMessage)
      }
    }
  })
})
```

**開啟package.json，增加npm scripts的設定，用來打包css**  
可用參數設定：
- --minify：是否最小化
- --source-map：是否產出source map檔案
- --input-folder：Sass程式碼資料夾
- --output-folder：打包後輸出css的資料夾
- --compass：是否載入compass

``` javascript
"sass:build": "node ./build --minify=true --source-map=true --input-folder=./sass/ --output-folder=./css/ --compass=true"
```

於終端機執行下列指令進行打包, 結果會輸出在畫面上
``` javascript
npm run sass:build
```
![build sass](//images.ctfassets.net/64scck6nxm6l/5RQ318crosW02fqcKuvIWm/bdc5ab7b40c77ecc8389b35d13e1df52/_09.png)

-------------

完整的專案程式碼(bitbucket)：[Link](https://bitbucket.org/TP-WenpinLiao/sassdemo/src/master/)
]]></content:encoded>
            <category>Sass</category>
            <category>compass</category>
        </item>
        <item>
            <title><![CDATA[物件導向實作課程筆記 Day 3]]></title>
            <link>https://monkeybinbin.github.io/article/csharp-oo-notes-day3</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/csharp-oo-notes-day3</guid>
            <pubDate>Sat, 24 Jun 2017 00:00:00 GMT</pubDate>
            <description><![CDATA[### 索引子
關鍵字 this 用來定義索引子  
不需要以整數值來索引,可以自行決定如何定義特定的查詢機制  
可以多載  
sample code：Xamarin.Android Listview使用的Adapter  
``` c
public class ParkAdapter: BaseAdapter
{
    private List _classA]]></description>
            <content:encoded><![CDATA[### 索引子
關鍵字 this 用來定義索引子  
不需要以整數值來索引,可以自行決定如何定義特定的查詢機制  
可以多載  
sample code：Xamarin.Android Listview使用的Adapter  
``` c
public class ParkAdapter: BaseAdapter<ClassA>
{
    private List<ClassA> _classA;
    public override ClassA this [int index]
    {
        get {
            return this._classA.ElementAt (index);
        }
    }
}
```  
索引子應用 Read Only Collection  
sample code
``` cs
public class ReadonlyPeople
{
    private List<Person> _items = new List<Person>();
    
    // 建構式
    public ReadonlyPeople()
    {
        SetDataToItems();
    }
    
    // 索引子
    public Person this[int index]
    {
        get { return _items[index]; }
    }

    // 產生集合內預設資料
    private void SetDataToItems()
    {
        _items = new List<Person>();
        _items.Add(new Person() { Name = "Andy", Age = 28 });
        _items.Add(new Person() { Name = "Bill", Age = 30 });

    }
    
    // 提供增加新項目的方法
    public void Add(Person person)
    {
        var result = _items.FirstOrDefault((x) => x.Name == person.Name);

        if (result == null)
        {
            _items.Add(person);
            Console.WriteLine("成功");
        }
        else
        {
            Console.WriteLine("姓名重複 !!!");
        }
    }
}
```

### 類別設計
#### 流程
需求→職責→抽象  
發散→收斂 重覆的執行(重構程式碼)  
<br/>
#### 目標
高內聚、低耦合  
相關的東西放在同一個class(相同的執行個體),相關的東西除了程式上也有可能是"意圖"  
一個依據"意圖"設計的例子就是我們時常用到的System.Math  
微軟將所有與計算相關的方法都放在System.Math之下  
讓開發人員能夠清楚的知道這些方法的用途並且也可以快速找到要用的方法  
<br/>
#### 技巧
用畫圖的方式幫助思考  
理解自已寫的程式,可以解釋程式為何要這樣設計(說的一嘴好程式)  
程式沒有絕對的寫法,依據需求撰寫多種解決方案再依照情境選擇較好的解決方案(平時練習或專案時間許可)  
重構很重要,當程式碼出現重覆的狀況就盡可能的進行重構  
盡可能降低方法的循環複雜度  
(降低方法的循環複雜度在撰寫測試案例時會有很大的助益)  
<br/>
#### 抽象類別與介面的選擇
**抽象類別**
重點在於重用性的設計  
描述一個同類物件的共通特性  
舉例：所有動物都具有吃、喝、睡覺等等共同的特性,不過每種動物吃的東西不一樣(人類什麼都吃，老虎吃肉...等),我們可以將這些基本特性,寫成抽象類別與方法讓其他類別來繼承並且實做方法  
**介面**
著重的則是抽象程度  
定義物件所需要具備的相同功能  
舉例：飛機會飛,鳥也會飛,但是兩者飛的方式不一樣。所以我們可以把飛寫成介面,物件只要繼承這個介面並且方法,該物件就具有飛行的特性了  
<br/>
### 設計模式
依據需求而反覆出現的各種問題,所提出來的解決方案  
如何在不重新設計下進行改變  
使用上可能會組合多種設計模式  
<br/>
### 繼承
繼承深度,建議不要超過3層  
#### 繼承缺點
侵入性的  
衍生類別會具有基底類別的所有特性,增加衍生類別的約束  
衍生類別會強耦和基底類別,當基底類別被修改也會影響衍生類別  
<br/>
### SOLID 六大原則
#### 單一職責原則(Single Responsibility Principle)
就一個類別而言，應該僅有一個引起它變化的原因  
#### 里式替換原則(Liskov’s Substitution Principle)
使用父類的地方，必須可以使用子類別代替，而不需要任何改變  
#### 依賴倒置原則(Dependency-Inversion Principle)
高層模組不應倚賴低層模組，兩者都應該倚賴抽象  
抽象不應該倚賴細節，細節應該倚賴抽象  
#### 介面隔離原則(Interface Segregation Principle)
客戶端不應該依賴它不需要的介面  
類別之間的依賴關係應該建立在最小的介面上  
#### 開閉原則(Open-Closed Principle)
對擴展開放,對修改封閉  
#### 最少知識原則(迪米特法則)(Law of Demeter)
一個物件應該對其他物件有最少的了解  

### Ioc(控制反轉)
控制移轉,將產生實體的控制權移轉到一個類別(factory)專門負責產生實體  
依賴抽象不依賴實作  
當產生執行個體的程式碼需要修改時，只需要修改專門負責產生實體的類別(factory)的程式碼，可以避免漏改或誤改  
<br/>

### DI(依賴注入)
當A Class有使用B Class的執行個體時,稱為A Class依賴B Class。將B Class的執行個體由A Class外部提供給A Class使用稱為DI  
<br/>
#### 注入方式
使用介面實作注入(Interface Injection)  
``` cs
public interface IDriver
{
    void Drive(ICar car);
}

public class Driver : IDriver
{
    public void Drive(ICar car)
    {
        car.Run();
    }
}
```
使用建構子注入(Constructor Injection)
``` cs
public class Driver
{
    private ICar _car;
    public Driver(ICar car)
    {
        _car = car;
    }

    public void Run()
    {
        _car.Run();
    }
}
```
使用屬性注入(Setter Injection)
``` cs
public class Driver
{
    public ICar Car
    { get; set; }

    public void Run()
    {
        Car.Run();
    }
}
```

### 共用的概念
使用同一份程式碼、共用抽象  
<br />
### 一般化與特殊化
一般化 => 父類別為子類別的一般化，因為繼承父類別後的子類別都具備與父類別相同的特性  
特殊化 => 子類別為父類別的特殊化，因為子類別除了擁有父類別的特性還可以自已擁有不同的特性  
如果子類別需要增加父類別沒有的其他特別方法時，建議使用"介面"繼承實作  
<br />
### 單例模式(Singleton)
確保某個類別只有單一執行個體,而且自行建立執行個體並向系統提供這個執行個體  
sealed class  
private靜態方法產生執行個體  
``` cs
public sealed class SingletonClass
{
    private int i = 0;
    // 設為private 外界不可產生執行個體
    private SingletonClass()
    {
    }

    public void Show()
    {
        Console.WriteLine("Hello " + i.ToString());
        i += 1;
    }

    private static SingletonClass _singletonObject;
    public static SingletonClass SingletonObject
    {
        get
        {
            if (_singletonObject == null)
            {
                GetSingleton();
            }
            return _singletonObject;
        }
    }
    
    // 靜態方法產生執行個體
    private static void GetSingleton()
    {
        _singletonObject = new SingletonClass();
    }
}
```

### 反射(Reflection)
由記憶體將"型別物件"拿出來看一看、用一用
#### 載入組件
Assembly.Load(by AssemblyName、by Assembly name string、by Assembly byte[])  
Assembly.LoadFrom  
Assembly.LoadFile  
<br />
#### 建立執行個體
AppDomain.CreateInstance  
AppDomain.CreateInstanceAndUnwrap  
Assembly.CreateInstance  
**Activator.CreateInstance**  
**Activator.CreateInstanceFrom**  
<br />
#### 利用反射存取成員
Member：Type.GetMember,Type.GetMembers  
Method：Type.GetMethod,Type.GetMethods,MethodBase.Invoke  
Property：Type.GetProperty,Type.GetProperties,PropertyInfo.SetValue,PropertyInfo.GetValue  
Interface：Type.GetInterface,Type.GetInterfaces  
<br />
#### 使用反射建立泛型實體
``` cs
// 載入GenericLibrary組件
Assembly asm = AppDomain.CurrentDomain.Load("GenericLibrary");
// 取得泛型類別
Type type1 = asm.GetType("GenericLibrary.GernericType`1");
// 指定泛型類別的參數具體型別
Type type11 = type1.MakeGenericType(new Type[] { typeof(String) });
// 建立泛型類別的執行個體
Object obj = Activator.CreateInstance(type11);
```

### Attribute
宣告式設計  
當Attribute被加入至某個元素時,該元素就被認為具有此特性的功能或性質  
被動的,無法存取目標物  
要建立一個可以當作Attribute的類別,必須繼承Attribute類別  
在執行階段可以使用"反射"來存取  
自訂Attribute類別
``` cs
internal class BoundaryAttribute : Attribute
{
    internal Double Max
    { get; set; }

    internal Double Min
    { get; set; }

    // 建構函式, 以便在套用 attribute 時初始化 Min, Max
    public BoundaryAttribute(int min, int max)
    {
        Max = max;
        Min = min;
    }
}
```
套用Attribute與取得Attribute sample code
``` cs
// 在列舉值中套用Attribute 寫法一
public enum GenderType
{
    [BoundaryAttribute(20, 25)]
    Man = 1,
    [BoundaryAttribute(18, 22)]
    Woman = 2
}

// 在列舉值中套用Attribute 寫法二
public enum GenderType
{
    [Boundary(20, 25)]
    Man = 1,
    [Boundary(18, 22)]
    Woman = 2
}

// 取得列舉值的Attribute資料
internal class EnumValueBoundryHelper
{
    internal Double Max { get; private set; } 
    internal Double Min { get; private set; } 
    public EnumValueBoundryHelper(GenderType gender) {
        FieldInfo data = typeof(GenderType).GetField(gender.ToString()); 
        Attribute attribute = Attribute.GetCustomAttribute(data, typeof(BoundaryAttribute));
        BoundaryAttribute boundaryattribute = (BoundaryAttribute)attribute;
        Min = boundaryattribute.Min;
        Max = boundaryattribute.Max;
    }
}

// 在型別上套用Attribute
[BoundaryAttribute(0, 100)]
public class BoundryClass
{
}

// 取得型別的Attribute資料
internal class ClassBoundryHelper
{
    internal Double Max { get; private set; }
    internal Double Min { get; private set; }
    public void GetBoundry(Type type)
    {
        if (type.IsDefined(typeof(BoundaryAttribute))) {
            Attribute attribute = type.GetCustomAttribute(typeof(BoundaryAttribute), true);
            BoundaryAttribute boundaryattribute = (BoundaryAttribute)attribute;
            Min = boundaryattribute.Min;
            Max = boundaryattribute.Max;
        }
    }
}
```

### 工廠模式
定義一個創建物件的介面  
分離物件的使用與建構+管理  
<br />
#### 簡單工廠(Simple Factory)
利用分支運算(if else,switch case)決定實體  
可以使用資源字典(利用key查找並提供對應的執行個體)或Attribute改善分支運算的問題(避免改錯分支運算的狀況)  
簡單工廠 sample code
``` cs
public static class StrategyFactory
{
    public static BMIStrategy GetStrategy(this Human human)
    {
        switch (human.Gender)
        {
            case GenderType.Man:
                return new ManBMIStrategy(human);
            case GenderType.Woman:
                return new WomanBMIStrategy(human);
            default:
                return new ManBMIStrategy(human);
        }
    }
}
```
#### 工廠方法(Factory Method)
由不同的工廠,決定不同的實體  
定義一個用於創建物件的介面,由此介面的子類別決定要實體化哪一個工廠  
工廠方法 sample code
``` cs
// 定義抽象工廠(介面)
public interface IBMIFactory
{
    BMIStrategy GetStrategy(Human human);
}

// 繼承抽象工廠與實作產生實體的方法,依需要實做多個工廠類別
public class ManBMIFactory : IBMIFactory
{
    public BMIStrategy GetStrategy(Human human)
    {
        return new ManBMIStrategy(human);
    }
}
public class WomanBMIFactory : IBMIFactory
{
    public BMIStrategy GetStrategy(Human human)
    {
        return new WomanBMIStrategy(human);
    }
}
```
使用工廠方法 sample code
``` cs
// 由不同的工廠,決定不同的實體
Human human = new Human() { Age = 19, Gender = GenderType.Woman, Height = 1.72, Weight = 58 };
IBMIFactory bmifactory = new WomanBMIFactory();
BMIStrategy bmistrategy = bmifactory.GetStrategy(human);
```
#### 泛型工廠(Generic Factory)
使用泛型與反射來產生不同的實體  
泛型工廠 sample code
``` cs
public class GenericFactory
{
    // 透過組件名稱,類別名稱(需含Namespace)產生指定類別的實體
    public static T CreateInastance<T>(string assemblyname, string typename)
    {
        object instance = Activator.CreateInstance(assemblyname, typename).Unwrap();
        return (T)instance;
    }
    // 透過Type產生指定類別的實體
    public static T CreateInastance<T>(Type type)
    {
        return CreateInastance<T>(type, null);
    }
    // 透過Type產生指定需要提供參數之類別的實體
    public static T CreateInastance<T>(Type type, object[] args) {
        object instance = Activator.CreateInstance(type, args);
        return (T)instance;
    }
}
```

### 範本方法模式(Template Method)
減少多餘的程式碼  
把通用實做放在基底類別  
範本方法模式 sample code
``` cs
// 定義當做範本的基底類別
public abstract class Communication
{
    // 通用實作,發送Command並回傳結果
    public byte[] SendCommand(byte [] command)
    {
        Send(command);            
        return Receive();
    }
    
    // 繼承此類別者必須實作Send與Receive抽象方法
    protected abstract void Send(byte[] command);
    protected abstract byte[] Receive();
}
```

### 策略模式(Strategy)
定義一組演算法,將每個演算法封裝,並且使它們可以互換  
策略模式封裝工廠模式 sample code
``` cs
public class BMIContext
{
    BMIStrategy _strategy;
    public BMIContext(Human human)
    {
        //封裝 Factory 建立實體的過程
        _strategy = StrategyFactory.GetStrategy(human); 
    }
    
    public Double BMI
    {
        get { return _strategy.BMI; }
    }
    
    public String Result
    {
        get { return _strategy.Result; }
    }
}
```

### 橋接模式(Bridge)
將抽象和實作解耦合,使其個別可以獨立變化  
簡單說就是用組合/聚合替代繼承  
橋接模式 sample code  
``` cs
// 計算BMI應提供的資料介面定義,身高、體重
public interface IMeasurementValue
{
    Double Weight
    { get; set; }
    Double Height
    { get; set; }
}

// BMI值計算的介面
public interface IBMIValue
{
    Double GetBMIValue(Double weight, Double height);
}

// BMI結果回應字串介面,不同的最大最小值與回應字串
public interface IBMIComment
{
    Double Min
    { get; }

    Double Max
    { get; }

    String GetBMIComment(Double bmi);
}

public class BMIBridge
{
    private Double _bmi;

    public IMeasurementValue Measurement
    { get; set; }

    public IBMIValue BMIValue
    { get; set; }

    public IBMIComment Comment
    { get; set; }

    public Double GetBMI()
    {
        _bmi = BMIValue.GetBMIValue(Measurement.Weight, Measurement.Height);
        return _bmi;
    }

    public string GetComment()
    {
        return Comment.GetBMIComment(_bmi);
    }
}
```

### 預設計與重構
面對熟悉的需求與情境,可以依據先前的經驗快速的進行預設計,免去曾經進行過的重構過程(縮短開發時間)  
如果是不熟悉的需求與情境,則可以先進行開發再透過重構改善程式碼的結構,以免發生過度設計的狀況  
<br />
### 心得
三天的課程非常的充實  
從基本概念到實際應用,也有很多程式碼的範例  
非常推薦剛開始學習寫程式或是已經寫程式一段時間的朋友可以聽聽看,相信會很有幫助  
<br />
\~ End \~]]></content:encoded>
            <category>CSharp</category>
        </item>
        <item>
            <title><![CDATA[物件導向實作課程筆記 Day 2]]></title>
            <link>https://monkeybinbin.github.io/article/csharp-oo-notes-day2</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/csharp-oo-notes-day2</guid>
            <pubDate>Sat, 17 Jun 2017 00:00:00 GMT</pubDate>
            <description><![CDATA[### override
可被覆寫的修飾詞 abstract、virtual與override  
若加上修飾詞 sealed後，該方法將不可再被覆寫  

### new
遮蔽的宣告方式  
盡量不要使用,違反[里氏替換原則（Liskov Substitution principle)](https://zh.wikipedia.org/wiki/%E9%87%8C%E6%B0%8F%E6%9B]]></description>
            <content:encoded><![CDATA[### override
可被覆寫的修飾詞 abstract、virtual與override  
若加上修飾詞 sealed後，該方法將不可再被覆寫  

### new
遮蔽的宣告方式  
盡量不要使用,違反[里氏替換原則（Liskov Substitution principle)](https://zh.wikipedia.org/wiki/%E9%87%8C%E6%B0%8F%E6%9B%BF%E6%8D%A2%E5%8E%9F%E5%88%99),而且很難預測實際執行的方法為何
可能使用情境 => 使用第三方元件,發現該元件某方法發生明顯的錯誤,則可透過遮蔽的方式進行"暫時"的修改,加上明確的說明註解並且盡快修正該元件。  
class定義
``` cs
public class Sample01
{
    public void Test()
    {
        Console.WriteLine("Sample01");
    }
}

public class Sample02 : Sample01
{
    public new void Test()
    {
        Console.WriteLine("Sample02");
    }
}
```
呼叫範例 => output為"Sample01"
``` cs
Sample02 sample = new Sample02();
Sample01 execSample = sample;

execSample.Test();
```

### 多載
同樣的方法名稱,不同的參數清單  
<br />
### 委派
delegate,是一種型別,可以宣告在namespace底下  
可以用來將方法當做引數傳遞給其他方法  
裡面儲存的都是方法指標  
多重的  
``` cs
namespace OOPSample
{
    public delegate void SomeAction(string message);
    class Sample
    {
        public void Main()
        {
            SomeAction action = Test01; // 增加委派的方法
            action += Test02; // 增加委派的方法(多重的)
            action += Test01; // 增加委派的方法(多重的)
            action.Invoke("早安"); // 叫用委派的方法

            action -= Test01; // 移除委派的方法(後加入的先移除)
            action("晚安");
        }

        public void Test01(string s)
        {
            Console.WriteLine($"Test01 {s}");
        }
        public void Test02(string s)
        {
            Console.WriteLine($"Test02 {s}");
        }
    }
}
```  

呼叫Main()的輸出  
![呼叫Main方法的輸出](http://user-image.logdown.io/user/11661/blog/11120/post/1956015/eklFsVC3SQaJxtQUUiY8_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-17%20%E4%B8%8B%E5%8D%8812.55.45.png)

### Action
無回傳值的委派,傳入的參數可以有0~16個  
<br />
### Func
有回傳值的委派,傳入的參數可以有0~16個  
回傳值型別一定在最後  
``` cs
// int 為方法回傳值的型別
Func<string, int> func;
```  

### 事件
event,讓類別或物件在某些相關的事情發生時，告知其他類別或物件  
一般event寫法  
``` cs
public class Sample01
{
    public event EventHandler XChanged; // 使用event
    
    private int _x;
    public int X {
        get { return _x; }
        set
        {
            if (_x != value)
            {
                _x = value;
                OnXChanged();
            }
        }
    }

    private void OnXChanged()
    {
        if (XChanged != null)
        {
            XChanged(this, new EventArgs());
        }
    }
}
```  
另一種寫法使用property  
``` cs
public class Sample02
{
    public EventHandler XChanged { get; set; } // 使用property

    private int _x;
    public int X
    {
        get { return _x; }
        set
        {
            if (_x != value)
            {
                _x = value;
                OnXChanged();
            }
        }
    }

    private void OnXChanged()
    {
        if (XChanged != null)
        {
            XChanged(this, new EventArgs());
        }
    }
}
```
#### 事件與事件委派函式
以Windows Form應用程式為例  
如果在Form上面的按扭button1,並撰寫Click應執行的程式碼時  
Form1.Designer.cs
``` cs
// Click為事件
// this.button1_Click為事件委派函式
this.button1.Click += new System.EventHandler(this.button1_Click);
```
Form1.cs 事件委派函式sample code
``` cs
private void button1_Click(object sender, EventArgs e)
{
    obj.X += 1;
}
```  

#### 帶有資料的宣告
自訂EventArgs  
``` cs
public class CustomEventArgs : EventArgs
{
    public int OldValue { get; set; } 
    public int NewValue { get; set; }
}
```  

#### 自訂委派
自訂委派sample code  
``` cs
public event CustomEventHandler XChanged;
private void OnXChanged(int oldvalue, int newvalue)
{
    if (XChanged != null)
    {
      XChanged(this, new CustomEventArgs()
      {
          OldValue = oldvalue, NewValue = newvalue
      });
    }
}
```
#### 使用EventHandler<T>替代自訂委派
使用EventHandler<T>  
``` cs
public event EventHandler<CustomEventArgs> XChanged;
private void OnXChanged(int oldvalue, int newvalue)
{
    if (XChanged != null)
    {
        XChanged(this, new CustomEventArgs()
        {
            OldValue = oldvalue, NewValue = newvalue 
        });
    }
}
```  

### 建構式
初始化物件的資料成員  
不使用任何參數的建構函式稱為「預設建構函式」(Default Constructor)  
建構式不會繼承(但會隱含呼叫父類的預設建構函式)  
當建構式有多載的狀況下,建議使用類別內部建構式呼叫的方式  
內部建構式呼叫sample  
``` cs
public class Sample01
{
    int x;
    int y;

    public Sample01() : this(10, 10)
    {
    }
    
    public Sample01(int a) : this(a, 10)
    {
        x = a;
    }
    
    public Sample01(int a, int b)
    {
        x = a;
        y = b;
    }
}
```  
繼承鏈上的建構式呼叫順序  
繼承鏈上的建構式呼叫順序(建立C類別的執行個體)  
``` cs
public class A
{
    private int _valueA;
    public A() { }
}
public class B : A
{
    private int _valueB;
    public B() { }
}
public class C : B
{
    private int _valueC;
    public C() { }
}
```  
上述程式碼的建構式執行流程圖  
![繼承鏈建構式執行流程圖](http://user-image.logdown.io/user/11661/blog/11120/post/1956015/y4XzxRSqTRaFjt7onda3_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-18%20%E4%B8%8A%E5%8D%8812.13.59.png)  

避免(一定不要)在建構式呼叫虛擬方法  
因為虛擬方法會依據執行個體來執行,有可能導致不可預期的錯誤  
![建構式呼叫虛擬方法範例](http://user-image.logdown.io/user/11661/blog/11120/post/1956015/xtPwJmY3QKqkTRsdkLR8_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-17%20%E4%B8%8B%E5%8D%882.58.16.png)  
<br />
### 靜態方法與執行個體方法的選擇
依據內聚性與情境的狀況來進行選擇  
舉例：  
如果是人的身高、體重、身份証字號為例,對每一個人來說都有自已的身高、體重、身份証字號應該為執行個體方法  
如果是總統因為全台灣的總統對台灣的所有人來說只有一位,所以應該使用靜態方法  
一個靜態方法與執行個體方法設計的選擇sample code  
``` cs
public class Rectangle
{
    public double Width { get; set; }
    public double Height { get; set; }
		
    // 合理的作法
    public double GetArea()
    {
        return Width * Height;
    }
    
    // 還算合理的作法(傳入一個Rectangle), 可抽出寫成擴充方法
    public static double GetArea(Rectangle rectangle)
    {
        return rectangle.Width * rectangle.Height;
    }
    
		// 不合理的作法，意圖不明確，使用的時候給的值可能會錯
    public static double GetArea(double width, double height)
    {
        return width * height;
    }
}
```

### 擴充方法
擴充方法是一種特殊的"靜態方法"，但是需將它們當成擴充類型上的執行"個體方法(Instance Method)"來呼叫。  
實作擴充方法sample code  
``` cs
public static class ExtensionClass
{
    public static string[] Splitline(this string str)
    {
        return str.Split(new string[] { Environment.NewLine },
            StringSplitOptions.None);
    }
}
```

### 介面
單一繼承 + 多介面實作  
介面是一系列方法、屬性、事件與索引的簽章  
介面不能定義執行個體欄位與建構式  
C# 不允許在介面中定義靜態成員  
介面的設計要儘量簡單  
#### 明確實作介面成員
當變數型別為明確實作介面型別時才看的到該實作的方法  
實務上比較少用到,MSDN文件比較常出現查閱時要特別注意  
<br />
#### 介面與抽象類別如何選擇？
如果沒有任何實作的需要就選擇介面,反之則選擇抽象類別  
介面主要是定義一些方法給彼此較不相關但卻有共同功能的類別使用  
而抽象類別主要應用在關係密切的類別使用  
另外要考慮的是一個類別能實作很多介面,但只能繼承一個抽象類別  
<br />
### 泛型
.Net Framework 2.0後才出現泛型  
泛型介面 interface Itest<T>  
泛型類別 class Test<T>  
泛型方法 void Test<T> (T value), T Test<T>()  
泛型委派 delegate void Del<T>(T item)  
#### default關鍵字
參考型別的 null  
實質型別的 0  
當沒有條件約束時泛型如何正確回傳  
<br />
## 泛型條件約束
將 new() 條件約束與其他條件約束一起使用時，一定要將其指 定為最後一個  
型別引數必須是實值型別  
``` cs
where T: struct
```
型別引數必須是參考型別
``` cs
where T : class
```
型別引數必須擁有公用的無參數建構函式
``` cs
where T : new()
```
型別引數必須本身是指定的基底類別，或衍生自該類別  
``` cs
where T : <base class name>
```
型別引數必須本身是指定的介面，或實作該介面  
``` cs
where T : <interface name>
```
提供給 T 的型別引數必須是 (或衍生自) 提供給 U 的引數  
``` cs
where T : U
```

### 共變性
用基底類別取代衍生類別  
<br />
### 逆變性
用衍生類別取代基底類別  
共變性與逆變性的差異  
``` cs
public class Gen0 { public int x; }
public class Gen1 : Gen0 {}

static void Main(string[] args)
{
    // 共變
    //Gen1 obj = OutMethod();
    Gen0 obj = OutMethod();

    // 逆變
    //InMethod(new Gen0());
    InMethod(new Gen1());
}

private static Gen1 OutMethod()
{ return new Gen1(); }

private static void InMethod(Gen0 obj) {}
```

### Lambda簡介與原理
#### LINQ Framework
LINQ to Object  
LINQ to ADO.NET(LINQ to Entities)  
LINQ to XML  
<br />
#### Lambda原理
~~其實Lambda是一種擴充方法,並且結合委派~~(錯誤，2017/06/20修正如下)  
應用"擴充方法"與"委派"  
經過一段時間的演進語法才會變的這麼簡潔(也是一種語法糖)  
Enumerable.Where
``` cs
public static IEnumerable<TSource> Where<TSource>
( this IEnumerable<TSource> source, Func<TSource, bool> predicate )
```
Enumerable.Where使用方法  
``` cs
List<string> data = new List<string>()
    {
        "bill", "david", "john"
    };
var result = data.Where((x) => x == "david");
```

### 迭代器(Iterator)
使用 yield 關鍵字時,表示關鍵字所在的方法、運算子或 get 存取子是迭代器  
當程式碼執行至yield return時,會將值回傳並記住目前程式碼的位置。下次再呼叫此Iterator時,就會從剛剛記住的位置重新開始執行。  
#### 使用注意事項
傳回類型必須是IEnumerable、IEnumerable<T>、IEnumerator 或 IEnumerator<T>  
不可包含任何 ref 或 out 參數  
<br />
#### 其他重點
優先採用 foreach 替代 for  
不可在 foreach 區塊內修改來源集合  
]]></content:encoded>
            <category>CSharp</category>
        </item>
        <item>
            <title><![CDATA[物件導向實作課程筆記 Day 1]]></title>
            <link>https://monkeybinbin.github.io/article/csharp-oo-notes-day1</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/csharp-oo-notes-day1</guid>
            <pubDate>Sat, 10 Jun 2017 00:00:00 GMT</pubDate>
            <description><![CDATA[抽象很重要，是在找出關鍵性特徵(與需求有關)

為什麼要依賴抽象？  
如果依賴具體時，面對需求變更會導致要改的東西很多而且程式碼愈改愈複雜  
所以如果依賴抽象，就可以**準時下班**(重點)  

人的思考方式其實就是一種抽象的概念  
例如平常看到柯基、博美…等，我們都會知道這些生物叫做狗  

## 物件導向三大特性
繼承 - C#只能繼承「一個」上層類別, 但可以繼承多個介面]]></description>
            <content:encoded><![CDATA[抽象很重要，是在找出關鍵性特徵(與需求有關)

為什麼要依賴抽象？  
如果依賴具體時，面對需求變更會導致要改的東西很多而且程式碼愈改愈複雜  
所以如果依賴抽象，就可以**準時下班**(重點)  

人的思考方式其實就是一種抽象的概念  
例如平常看到柯基、博美…等，我們都會知道這些生物叫做狗  
<br />
## 物件導向三大特性
繼承 - C#只能繼承「一個」上層類別, 但可以繼承多個介面  
封裝 - 隱藏不必要被外界所知的資訊、隱藏行為的變化  
多型 - 繼承式、參數式(泛型)、多載(overloading-程序、運算子)、強制同型(與編譯有關)  
<br />
## 型別與變數
int與System.Int32有什麼不同？  
編譯後的結果一樣。  
int定義在C#  
System.Int32定義在.Net Framework  
<br />
### 型別概論
Primitive Type  
Reference Type - Interface、Class、Delegate  
Value Type - Structure、Enum  
<br />
### 實值型別
變數內容就是物件本身  
存在於 Stack  
<br />
### 參考型別
變數內容則是儲存指向物件的參考(指標)  
存在 Heap  
<br />
### 實值型別與參考型別物件比較
![實值vs參考型別](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/rW5vt73SAKlvbaFwdfsY_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-10%20%E4%B8%8B%E5%8D%8811.45.54.png)
<br />
### 變數在記憶體中複製的行為
範例程式碼
``` cs
int a = 10;
int b = a;
```

1.產生變數a的記憶體位址與值。  
![01](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/iE2hkxqTu2qAIXKtnsSB_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-10%20%E4%B8%8B%E5%8D%889.18.31.png)

2.複製變數a的值。  
![02](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/H5Nf9BP8R7C6qW7W8jQI_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-10%20%E4%B8%8B%E5%8D%889.23.09.png)

3.產生變數b的記憶體位置，並將步驟2複製出來的值放到變數b的值。  
![03](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/9VpWgvAZSkidRPiy5Rr2_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-10%20%E4%B8%8B%E5%8D%889.24.55.png)

4.刪除步驟2複製出來的值。  
![04](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/mLpXzEAORGitqkhONrty_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-10%20%E4%B8%8B%E5%8D%889.27.20.png)
<br />
## var
強型別  
隱含型別(語法糖)  
右(後)決議型別  
只能做為宣告區域變數使用  
<br />
## Eunm
幫助記憶 - 使用具有代表意義的文字取代常數  
限制 - 限制輸入的範圍  
<br />
## FlagsAttribute
列舉值應為bit
``` cs
[Flags]
public enum Authority
{
  Read = 1,
  Write = 2,
  Create = 4,
  Delete = 8
}
```
  
## Structure
自訂的複合(Compositing)實值型別  
開發遊戲(想避免GC影響的時候才比較需要使用)  
Nullable<T>也是一種Structure, T僅限使用實值型別  
<br />
## Boxing與UnBoxing
實值型別與參考型別之間的轉換  
造成效能耗損  
<br />
## Class
### 存取修飾詞
在命名空間中宣告的類別可為 public 或 internal(預設),而且只有型別可以宣告  
類別內的成員可以宣告為  
private(預設)  
internal - 同一個組件看的到  
protected - 繼承者看的到  
protected internal - 同一個組件或繼承者看的到  
public  
<br />
### 其他修飾詞
abstract(抽象)  
sealed(密封, 不可被繼承)  
<br />
### 參考型別物件與執行個體物件比較
Static fields儲存於「參考型別物件」內  
Method在記憶體內都是獨立唯一, 也儲存於「參考型別物件」內  
所以Static Method與一般的Method差別只在呼叫的方式不同  
![參考型別物件vs執行個體物件](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/R7n3nXMTvCi8Qw7hr2Ag_%E8%9E%A2%E5%B9%95%E5%BF%AB%E7%85%A7%202017-06-10%20%E4%B8%8B%E5%8D%8811.55.55.png)  
<br />
### 常數
const宣告, 同時必須初始化  
不可修改  
常數如果有修改, 如果有參考到該dll者都需要重新編譯   
<br />
### 欄位
在.Net, 類別層級的變數  
一般情境下, 欄位的存取層級很少是public(readonly例外)  
<br />
### 屬性
方法的變形  
使用屬性取代欄位成為公開介面, 保護欄位  
<br />
### 方法參數傳遞
實值型別傳值  
![實值型別傳值](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/CRVVxy15RUaubjGFrKk9_01.png)  
實值型別傳址  
![實值型別傳址](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/HlwH1m2kS5SukBkoyKtm_02.png)  
參考型別傳值  
![參考型別傳值](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/l5u83XqVQIe1PJnvoOk4_03.png)  
參考型別傳址  
![參考型別傳址](http://user-image.logdown.io/user/11661/blog/11120/post/1937727/1QJPmSwGSOSjITjCc7x8_04.png)  
Out宣告(例如：xxx.TryParse)  
params宣告(例如：String.Format)  
<br />
參考資料  
[利用Visual studio觀察記憶體內容](https://dotblogs.com.tw/initials/2017/01/28/a00_basis)
]]></content:encoded>
            <category>CSharp</category>
        </item>
        <item>
            <title><![CDATA[Jenkins 安裝 nodejs 設定執行 npm script 任務]]></title>
            <link>https://monkeybinbin.github.io/article/jenkins-set-npm-script-task</link>
            <guid isPermaLink="false">https://monkeybinbin.github.io/article/jenkins-set-npm-script-task</guid>
            <pubDate>Thu, 25 May 2017 00:00:00 GMT</pubDate>
            <description><![CDATA[1.登入Jenkins,開啟「管理外掛程式」功能。  
![01.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/JcwyD5R5QWmZbpXm4VYO_01.png)

2.搜尋Nodejs,直接安裝NodeJS Plugin。  
![02.png](http://user-image.logdown.]]></description>
            <content:encoded><![CDATA[1.登入Jenkins,開啟「管理外掛程式」功能。  
![01.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/JcwyD5R5QWmZbpXm4VYO_01.png)

2.搜尋Nodejs,直接安裝NodeJS Plugin。  
![02.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/cc9D7LoKQQi9YUKRdl5q_02.png)

3.安裝成功後選擇回到首頁或重啟Jenkins,如選擇重啟待重啟完畢後重新登入Jenkins。  
![03.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/CxSnlOEQQMSdP6gItxT1_03.png)

4.開啟「Global Tool Configuration」。主要是為了安裝node js(與npm)。  
![06.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/26kofB3VQRyk9vcutQrP_06.png)

5.將畫面捲動至NodeJS區塊，按下「新增NodeJS」。  
![07.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/9tpOpJwoSRif9pM5mspM_07.png)

6.畫面會出現詳細的安裝設定，請依照需求進行安裝設定。可同時安裝多種版本的node js，可依據需要指定不同版本的node js來執行相關指令。設定完成後按下「Save」或「Apply」即完成node js安裝。  
![08.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/hX1URONAQeG2H5Nu5xEz_08.png)

7.接下來建立一個新的作業(點擊「新增作業」)，作業內容是從github取得程式碼，再執行npm script進行自動測試與打包。  
![09.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/QwFhOclSMWCQOQcoZoPg_09.png)

8.進行作業的組態設定，首先設定「原始碼管理」，指定原始碼來源，此處範例為public的github專案，若要取得private專案可透過帳號密碼或是ssh key…等方式進行存取。  
![11.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/YjYQNFMSYGtEmIXVQk0p_11.png)

9.設定「建置環境」，勾選「Provide Node & bin/folder to PATH，選擇前面步驟「6」所安裝/設定的node js版本。  
![12.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/I9UywIENQQiqseMzGACZ_12.png)

10.設定「建置」，新增建置步驟「執行 Shell」。  
![13.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/k45NfstTTUSsTBcUafZw_13.png)

11.輸入平時打包應執行的npm指令，此處主要有3個指令。分別是為了安裝npm的package、測試以及程式打包。設定完成後，按下「Save」即完成設定，頁面會自動跳轉至此作業的管理畫面。  
![14.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/izqSvncVS5SAOOqVpvCn_14.png)

12.按下「馬上建置」，測試作業設定是否正確。  
![15.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/LuHc52dRx6syS5KZtPQG_15.png)

13.建置歷程會出現此次執行的項目，按下項目前方的灰色燈號可以進入console output查看此作業執行的輸出資訊。  
![16.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/8RqDR1ghTpWzU9x3mRtH_16.png)  
console output畫面如下  
![17.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/JAyIykmETe6rVRrZ6Biu_17.png)

14.作業完成畫面。  
![18.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/lOTrFu7jQmSFBeD5H2kf_18.png)

15.點選工作目錄，可找到webpack設定的輸出目錄與打包的結果檔案，表示此作業已經成功的完成所有打包動作。後續即可自動布署至主機。  
![19.png](http://user-image.logdown.io/user/11661/blog/11120/post/1870651/fqUUHdmyTfW6BUNrb6p3_19.png)

\~ End \~]]></content:encoded>
            <category>Jenkins</category>
        </item>
    </channel>
</rss>